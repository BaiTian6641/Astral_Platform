# Ethereal Logic Platform — 机器可读任务清单 v2.1
# 与《Ethereal-平台实施蓝图-v2.md》及《Ethereal-蓝图v2.1-BMC与运行时修订.md》配套。
# v2.1 变更：弃用 GW5 AE350 硬核（付费工具链）；管理单元改为 fabric 内 RISC-V 软核（BMC，
# 主选 NEORV32），小器件降级为 mFSM（与 BMC 统一 EMRI 寄存器 ABI）。
# 状态同步：2026-09-01 与 git 历史/验收报告对齐（status: done/in_progress 依据 docs/reports/ + 提交记录）。
# 字段说明：
#   id / title / phase / deps / est_days / deliverable / acceptance / status

project:
  name: Ethereal Logic Platform
  decisions:
    route: overlay-first (fine-grained virtual reconfigurable fabric)
    platforms: [gowin-gw5ast-138, zynq-ultrascale-plus]
    licenses:
      { software: MIT, hardware_rtl: CERN-OHL-S-2.0, docs_specs: CC-BY-SA-4.0 }
    bus: EBI (AXI4-Lite + custom NoC + tiny fallback)
    host_link: { data: SPI, monitor: I2C (PMBus-style) }
    control_plane: independent-then-unified (ADR-010)
    management:
      bmc: "soft-core RISC-V in fabric (primary: NEORV32, fallback: VexRiscv)" # ADR-013/016
      small_devices: mFSM (register-based, host-driven) # ADR-014
      unified_abi: EMRI (BMC and mFSM expose identical register interface) # ADR-015
      deprecated: gowin-ae350-hardcore (paid toolchain, unusable software)

phases:
  - id: phase-0
    name: 基础设施与仿真验证
    window: M0-M2
    exit_criteria:
      - 仿真内双镜像热替换演示通过
      - AES-128 与 FIR16 基准在虚拟 fabric 上 bit-true
      - MAP 路线决策 ADR-012 完成
      - CI 全绿
    circuit_breaker: VPR 架构 2 周不收敛则转 FABulous/nextpnr 或自研 placer+router（各 5 人日上限）

  - id: phase-1
    name: GW5 bring-up 最小闭环（BMC 软核版）
    window: M2-M5
    exit_criteria:
      - BMC 软核运行 daemon，ethctl run/stop/ps/restart 全通
      - 2 region x 10000 次热替换零故障
      - v0.1.0 公开发布（视频+博客+文档站）
    circuit_breaker: BMC 固件通道受阻则临时以上位机直驱 EMRI/OCC 保底（mFSM 语义先行）

  - id: phase-2
    name: 异构 fabric v2 + Zynq 移植 + mFSM + Astral 聚合 v1
    window: M5-M9
    exit_criteria:
      - 同一 logic image 在 GW5 与 Zynq US+ 直接运行（二进制兼容）
      - 异构 tile 基准报告（AES-MEM 密度 >=5x, FIR-DSP 吞吐 >=10x）
      - mFSM 在仿真中由上位机驱动完整部署流程
      - Astral Type-F 聚合演示
      - EBI/镜像/BoardManifest/EFP 规范 v1.0 冻结

  - id: phase-3
    name: Service Tile + 调度 + 安全 + 学术发布
    window: M9-M15
    exit_criteria:
      - NPU-Tiny 推理 demo + 多容器分时复用
      - 容器迁移/抢占演示（BMC 编排上下文保存）
      - OCI 镜像仓库 push/pull
      - 论文投稿（FPL/FCCM/TRETS）

  - id: phase-4
    name: 编排与生态
    window: M15-M24
    exit_criteria:
      - 统一编排器（bundle/多板/声明式）
      - BMC 固件迁移 Zephyr，BMC 成为 Astral 节点
      - 4 个参考设计套件
      - ">=2 名核心外部贡献者"

  - id: phase-5
    name: 商业级演进
    window: M24+
    exit_criteria:
      - 企业版编排/RBAC/远程运维（BMC 健康审计为卖点）
      - 安全认证预评估材料
      - 镜像仓库 SaaS / 厂商合作

tasks:
  # ================= PHASE 0 =================
  - id: E0-INF1
    title: GitHub 组织与 8 仓库骨架（许可证/DCO/CONTRIBUTING）
    phase: phase-0
    deps: []
    est_days: 1
    deliverable: 仓库组（ethereal-fabric/shell/tools/runtime/spec/images/astral-os/docs）
    acceptance: clone 后 README 与许可证链接全部有效；DCO 检查启用
    status: done

  - id: E0-INF2
    title: CI 骨架（Verilator lint + cocotb 回归 + 文档构建）
    phase: phase-0
    deps: [E0-INF1]
    est_days: 1
    deliverable: GitHub Actions 工作流
    acceptance: dummy RTL 测试在 CI 变绿
    status: blocked   # 2026-09-13 对账: 本机可做部分全部完成（工作流解析、按 CI 口径复跑 Python 步骤、make lint 绿、sim 镜像构建修复已提交 a489bff）。**远程首次真实运行已发生**（维护者 push 69ba9c3 触发 run 34696652313）：`Python tools lint` 与 `Docs` 绿，`Lint and Test` 卡在 “Build sim Docker image”，已修 3 个缺陷（python3.12-distutils 已从 deadsnakes 消失、YOSYS_TAG 应为 v0.59、timeout 30→240 且加 GHA 层缓存）⇒ 剩余一步 = 维护者下次 push 后该 job 变绿（本机无 docker 无法复现构建）

  - id: E0-INF3
    title: 仿真环境 Dockerfile（Verilator5 + cocotb + Yosys + VPR8）
    phase: phase-0
    deps: [E0-INF1]
    est_days: 2
    deliverable: Dockerfile + Makefile（make sim）
    acceptance: 干净机器 30 分钟内复现仿真环境
    status: done

  - id: E0-INF4
    title: 商标/名称可用性检查（Ethereal/Astral）
    phase: phase-0
    deps: []
    est_days: 0.5
    deliverable: 检查记录文档
    acceptance: GitHub org/域名/开源商标冲突结论明确
    status: done

  - id: E0-FAB1
    title: eLUT4+FF 单元 RTL
    phase: phase-0
    deps: [E0-INF3]
    est_days: 1
    deliverable: rtl/clb/elut4.sv + cocotb 测试
    acceptance: 随机真值表 1000 组比对通过
    status: done

  - id: E0-FAB2
    title: CLB-T cluster RTL（N=8, Clos IIB, 参数化）
    phase: phase-0
    deps: [E0-FAB1]
    est_days: 3
    deliverable: rtl/clb/clb_t.sv
    acceptance: cluster 内 LUT→LUT 连通性穷举通过
    status: done

  - id: E0-FAB3
    title: Switch box + 通道互联（W=12, 两源轨道优先）
    phase: phase-0
    deps: [E0-FAB2]
    est_days: 4
    deliverable: rtl/interconnect/ + 拓扑表
    acceptance: 4x4 cluster 网格例化成功，无组合环
    status: done

  - id: E0-FAB4
    title: 配置帧组织 + OCC v0（WRITE/BLANK/READBACK）
    phase: phase-0
    deps: [E0-FAB3]
    est_days: 4
    deliverable: rtl/occ/ + 帧地址映射生成脚本
    acceptance: 经 OCC 写配置后 fabric 行为=指定电路（加法器/计数器样例）
    status: done

  - id: E0-FAB5
    title: blank-before-write 与 region 配置锁
    phase: phase-0
    deps: [E0-FAB4]
    est_days: 2
    deliverable: OCC 扩展 + 断言
    acceptance: 改写相邻 region 时本 region 输出无毛刺（断言监测）；LOCK 后写被丢弃
    status: done

  - id: E0-FAB6
    title: fabric-gen v0（fabric.yaml → 顶层 Verilog + 帧映射 JSON）
    phase: phase-0
    deps: [E0-FAB3]
    est_days: 3
    deliverable: tools/fabric_gen/
    acceptance: 2x2 与 4x4 两种 fabric 均通过 E0-FAB4 验收用例
    status: done

  - id: E0-MAP1
    title: Yosys 定制综合（eLUT4 techlib + synth_ethereal）
    phase: phase-0
    deps: []
    est_days: 2
    deliverable: tools/mapper/yosys/
    acceptance: c17/c432 映射为 eLUT4 网表，LUT 数合理
    status: done

  - id: E0-MAP2
    title: VPR 架构文件（eLUT4 cluster + W=12）
    phase: phase-0
    deps: [E0-FAB2, E0-FAB3]
    est_days: 4
    deliverable: tools/mapper/vpr/arch_ethereal.xml
    acceptance: VPR 对 c432 完成 pack/place/route，时序报告可读
    status: done

  - id: E0-MAP3
    title: bitgen v0（VPR 结果 → 配置帧比特流）
    phase: phase-0
    deps: [E0-MAP2, E0-FAB4]
    est_days: 5
    deliverable: tools/mapper/bitgen/
    acceptance: c432 端到端载入仿真 fabric，输出与参考模型 bit-true
    status: done

  - id: E0-MAP4
    title: FABulous 流程评估 spike → ADR-012（MAP 路线决策）
    phase: phase-0
    deps: []
    est_days: 3
    deliverable: 评估笔记 + docs/adr/ADR-012.md
    acceptance: 路线 A(VPR) / B(FABulous) 决策记录完成
    status: done

  - id: E0-MAP5
    title: 基准电路集（AES-128/PRESENT/FIR16/CRC32/PWM + 黄金向量）
    phase: phase-0
    deps: [E0-MAP3]
    est_days: 4
    deliverable: benchmarks/
    acceptance: 全部基准经完整流程在仿真 fabric 运行正确
    status: done

  - id: E0-SHL1
    title: EBI-Tiny 总线 RTL + 地址映射 decoder
    # D1 (2026-09-01, report-phase0-closeout): Phase-0 Shell 主控路径已由 ADR-018 自研 AXI
    # 链（EMRI-over-AXI）实现；EBI-Tiny 降为 ADR-006/018 的小器件降级 profile，随 mFSM（E2-BMC1）落地。
    phase: phase-0
    deps: []
    est_days: 1
    deliverable: rtl/ebi/ebi_tiny.sv
    acceptance: BFM 随机读写一致性测试通过
    status: done   # 2026-09-13 由 E2-BMC1 收口: 计划原本即注明「EBI-Tiny 降为小器件降级 profile，随 mFSM（E2-BMC1）落地」。交付 `ethereal-shell/rtl/ebi/ebi_tiny.sv`（+ `ebi_pkg.sv` 的 ADR-006 profile 与窗口图）；验收「BFM 随机读写一致性测试通过」已达成：TB TEST PASSED（13,944 checks / 10,000 ops，各窗口提交数==写入数，6 个额外种子，Verilator 下同数），金模型 45 pytest，verilator -Wall 0 警告无豁免；Makefile 已接线（RTL_CLEAN/deps/test-sv）

  - id: E0-SHL2
    title: Shell v0 仿真集成（EBI+OCC+fabric+宿主 BFM）
    phase: phase-0
    deps: [E0-FAB4, E0-SHL1]
    est_days: 3
    deliverable: sim/tb_shell/
    acceptance: 仿真内完成完整容器部署周期：写配置→启动→读结果→blank→换镜像→再运行
    status: done   # 2026-09-01: tb_mgmt_hotswap + tb_bmc_axi_fabric 经 EMRI/OCC 驱动真实 fabric_top 完成双镜像热替换（host BFM 直驱；C 固件驱动版随 E1-RUN2）

  - id: E0-SHL3
    title: 性能建模（配置字节数/时延模型/虚拟 Fmax 初值）
    phase: phase-0
    deps: [E0-SHL2]
    est_days: 2
    deliverable: docs/performance-model.md
    acceptance: 给出 4x4 fabric 配置字节数与 Fmax 估算并归档
    status: done   # 2026-09-01: docs/performance-model.md + perf_model.py（数字可复现，含断言自检）；report-E0-SHL3-perfmodel-20260901

  # ================= PHASE 1 =================
  - id: E1-PLT1
    title: GW5 宿主映射层 hal/gowin_gw5（CFU memory 模式/BSRAM/DSP wrapper）
    phase: phase-1
    deps: [E0-FAB6]
    est_days: 4
    deliverable: ethereal-fabric/hal/gowin_gw5/
    acceptance: 4x4 fabric 综合后物理 LUT 开销 ≤45:1 并报告实际值
    status: done   # 2026-09-01: hal/gowin_gw5 交付（glue+stub+probe）；实测 308:1 @4x4 —— ≤45:1 未达（互联占 91%：CB 47%/IIB 34%/SB 10%，无摊薄渐近 ~310-350:1），偏差转入 E2-FAB5 并待维护者签收；report-E1-PLT1-hal-gowin-20260901

  - id: E1-PLT2
    title: Base image 构建流（Gowin EDA 工程模板 + 脚本化）
    # 2026-09-01 评估：开源链（yosys+nextpnr-himbaechel+gowin_pack，E1-PLT4）可替代 Gowin EDA 出 .fs；
    # openFPGALoader 支持 tangmega138k。真正阻塞 = ① GW5 SoC 顶层（BMC+EMRI+fabric+SPI/I2C 板级集成，
    # 待 E1-BMC1/E1-IO1 落地）② 实体板卡在维护者处（烧写+SPI 读出 magic 无法仿真替代）。
    phase: phase-1
    deps: [E1-PLT1]
    est_days: 3
    deliverable: build/gowin/ + 构建脚本
    acceptance: Tang Mega 138K 烧写后经 SPI 读出 Shell magic number
    status: todo

  - id: E1-PLT4
    title: Apicula/nextpnr Aurora V 备选构建链评估
    phase: phase-1
    deps: [E1-PLT1]
    est_days: 3
    deliverable: 评估笔记
    acceptance: 可行性结论（CI 构建通道是否可用）
    status: done   # 2026-09-01: 全链打通 yosys synth_gowin→nextpnr-himbaechel(GW5AST-138C chipdb)→gowin_pack→.fs；Fmax 115.61MHz@4x4 fabric；LUT 开销实测 ~308:1（vs E1-PLT1 目标 45:1，转入 E2-FAB5 优化输入）；report-E1-PLT4-nextpnr-spike-20260901

  - id: E1-BMC1
    title: BMC SoC 集成（NEORV32 + Boot ROM + 128KB FW SRAM + UART + EBI 桥）
    phase: phase-1
    deps: [E0-SHL1]
    est_days: 4
    deliverable: ethereal-shell/bmc/（bmc_core wrapper，核可替换）
    acceptance: 仿真与 GW5 双平台 hello-world + Shell CSR 读写；BMC 总开销报告（LUT+BSRAM）
    status: blocked   # 2026-09-13 对账: 仿真半边已完成（IMEM 重生成 + tb_bmc_hello 字节级等价 + >1KiB 执行探针 + daemon/lock/DMA/脚本部署全链路 TB 绿）；**GW5 平台半边依赖 E1-PLT2**（板级/约束），故本任务阻塞而不再 in_progress

  - id: E1-BMC2
    title: BMC 固件框架（boot 双分区 + 驱动层 + lifecycle 状态机骨架）
    phase: phase-1
    deps: [E1-BMC1]
    est_days: 4
    deliverable: ethereal-runtime/bmc-fw/
    acceptance: 固件 UART 启动；region 状态机空转演示；FW 双分区自更新演示
    status: done   # 2026-09-11 (sim 范围): 三条验收证据齐 —— ① UART 启动（tb_bmc_fw）② region 生命周期（tb_bmc_daemon 7 阶段 41 检查）③ FW 双分区自更新（bmc-fw/fwupdate/ + tb_bmc_fwupdate 30/30，emri-v0 §3.6 v0.4）。生产 daemon 对 EFP_CMD 6/7 应答 bad_cmd（demo 为独立固件变体）；reboot 为模拟（netlist 无核内软复位，ASSUMPTION 已记录）。report-E1-BMC2-fwupdate-20260911；GW5 板级验证随 E1-PLT2

  - id: E1-BMC3
    title: BMC 调试通道（UART 控制台 + JTAG 排针 + OpenOCD 配置）
    phase: phase-1
    deps: [E1-BMC1]
    est_days: 2
    deliverable: 调试指南 + openocd cfg
    acceptance: GDB 断点/单步调试 BMC 固件成功；全程无付费工具
    status: todo

  - id: E1-BMC4
    title: EMRI 寄存器面实现（ADR-015）+ 上位机模式探测
    phase: phase-1
    deps: [E1-BMC1]
    est_days: 2
    deliverable: rtl/emri/ + ethctl 探测逻辑
    acceptance: ethctl 自动识别 BMC/mFSM 模式并调整会话语义
    status: done   # 2026-09-13 收官: EMRI 寄存器面（emri_regfile + 全套 TB）+ 能力探测（CAPABILITIES.has_bmc，ethctl info 输出 has_bmc）此前已完成；本轮补齐**自动识别与按模式调整会话语义**：`--transport {auto,mfsm,efp}` 默认 auto（探测后分派 EFP mailbox 或 host 驱动的 Daemon）、`--device {auto,emri,bmc}`、显式选择时对探测矛盾 fail-closed、旧 `--transport efp` 隐式映射到 bmc（既有调用不变）。模式差异全部按 `emri-v0.md` §1.1/§3.2/§3.8/§5/§8 落地：谁发起 `OCC_CMD.start`、谁验 Ed25519、会话 FSM 归属、mFSM 上 EFP mailbox 无效（拒绝）、mFSM 下 `MON_NOTIFY` 由**主机**在 OCC_DONE 后写入、daemon 专属流程（stop/restart/abort、run_packed、ctx save/restore、fwupdate/reboot）在 mFSM 上拒绝、`inspect` 跨模式逐字节一致。14 条新测试；scoped 61 passed；全量 `pytest ethereal-tools` 在 OSS-CAD PATH 下 **514 passed / 0 failed**；ruff/mypy --strict 干净（其余历史 mypy 债已如实记录，未改任何断言）。三处规范歧义按 G6 报告未猜（region-lock 生命周期是否需 mFSM 主机镜像、节流预检归属、v0 的 SESSION_CMD 是否脉冲）

  - id: E1-IO1
    title: SPI 数据通道（NEORV32 SDI + EFP-SPI 帧协议固件栈 + CRC16）
    phase: phase-1
    deps: [E1-BMC2]
    est_days: 2
    deliverable: bmc-fw/efp-spi/ + spec EFP-SPI
    acceptance: 上位机经 SPI 完成完整 logic image 部署；CRC 错误注入通过
    status: done   # 2026-09-02（仿真范围）: SDI netlist 换入（16KiB IMEM + SDI，hello 字节级等价）+ bmc_core SDI 端口 + 10 TB 后门换名 + bmc-fw/efp-spi 固件栈 + §7.1 CRC16 附录 + tb_bmc_spi 全链通过（SPI 帧驱动完整 packed 部署 + CRC 注入拒绝）；report-E1-IO1-efp-spi-20260902；实板 Raspberry Pi 联调待 E1-PLT2

  - id: E1-IO2
    title: I2C 监控通道 v1（NEORV32 TWD + PMBus 风格命令固件）
    phase: phase-1
    deps: [E1-BMC2]
    est_days: 2
    deliverable: bmc-fw/monitor/ + spec RFC-003
    acceptance: 树莓派 i2cget 读出 slot 状态；24h 轮询无锁死
    status: todo

  - id: E1-IO3
    title: L1 引脚 Mux v1（8 脚组 x4）+ Board Manifest
    phase: phase-1
    deps: [E0-SHL1]
    est_days: 3
    deliverable: rtl/io/pinmux/ + boards/tang-mega-138k.yaml
    acceptance: PWM 镜像经 mux 输出到任意两组引脚，示波器验证
    status: todo

  - id: E1-IO4
    title: L2 协议代理 v1（UART + GPIO）
    phase: phase-1
    deps: [E0-SHL1]
    est_days: 3
    deliverable: rtl/io/proxy_uart/ + proxy_gpio/
    acceptance: region 内虚拟逻辑经 UART 代理 115200 收发无丢字节
    status: todo

  - id: E1-RUN1
    title: ethimg 镜像工具（pack/unpack/verify + Ed25519）
    phase: phase-1
    deps: [E0-MAP3]
    est_days: 2
    deliverable: tools/ethimg/
    acceptance: 篡改任意字节即验签失败
    status: done

  - id: E1-RUN2
    title: ethereal-daemon v1（BMC 固件：验签→region 分配→OCC(DMA) 加载→生命周期）
    phase: phase-1
    deps: [E1-BMC2, E1-IO1]
    est_days: 4
    deliverable: bmc-fw/daemon/
    acceptance: run/stop/ps/restart 全通；坏签名/满 region/写冲突优雅报错
    status: done   # 2026-09-01: EMRI v0.2 EFP 命令块 + C daemon（Ed25519 验签→分配→OCC→生命周期）+ tb_bmc_daemon 7 阶段全过（41 ok 检查，主 Agent 独立复跑）；region_locked 路径待 E1-RUN3 锁矩阵（待确认#3）；report-E1-RUN2-daemon-20260901 **v0.3 增量（2026-09-02）**：run_packed 生产帧部署（EFP_IMG_COLS@0x21 + 逐列 OCC_DECODE 循环）+ tb_bmc_daemon_packed 26 检查全过（真实 fabric TFF 翻转 + 篡改签名拒绝 + stop 逐列 blank）；report-E1-RUN2-packed-deploy-20260902

  - id: E1-RUN3
    title: ethctl CLI（Docker 风格命令集）
    # 2026-09-01 范围注记：sim v0 = EFP 客户端 + 会话脚本回放（v0 cfg-addr 帧格式）。
    # 生产 bit-packed .eth（bitgen_pack 全帧）部署需 daemon 增加 packed-deploy 模式
    # （按列 OCC_DECODE 循环，spec §3.1）——列为本任务的后续增量，验收以实板 30s 为准。
    phase: phase-1
    deps: [E1-RUN2, E1-BMC4]
    est_days: 3
    deliverable: tools/ethctl/
    acceptance: ethctl run aes128.eth --region 0 在 30s 内完成部署
    status: done   # 2026-09-02(sim 范围): efp_client.py + ethctl --transport efp/--emit-session + tb_ethctl_replay（ethctl 会话回放驱动真实 daemon 链 run/stop 全过）；修出 eth_axi_xbar 跨主同目的响应路由混叠真 bug；27 pytest 全过；packed-deploy 后续增量见上方注记；report-E1-RUN3-ethctl-efp-20260901

  - id: E1-RUN4
    title: region 看门狗 v1（心跳+超时 blank+事件日志）
    phase: phase-1
    deps: [E1-RUN2]
    est_days: 2
    deliverable: bmc-fw/watchdog/
    acceptance: 死锁镜像超时自动 blank，相邻 region 无损
    status: done   # 2026-09-11 (sim 范围): OCC op watchdog（2^20 预算 → drain-complete + BLANK + EFP_ERR=9 + 事件 code 1）+ region 心跳 + 事件环（§3.4）；tb_bmc_watchdog 29/29 全过（含邻区无损）。过程中修出并经 ADR-019/spec v0.5 解决两个真问题：① frame_decoder.frame_base_i 全 TB 硬接 0 → region>0 部署错解；② occ_top 单一 write_crc_r「最后写赢」→ 心跳误隔离健康 region 且 cols>1 部署必然失败 → 引入软件供给的 OCC_EXPECT_CRC/OCC_CRC_RESULT（§3.1.1）。回归：tb_bmc_daemon 39 / packed 25 / spi 31 / replay 18 / fwupdate 30 全过，模型 2683+2xfail，lint/ruff clean；report-E1-RUN4-watchdog-20260911、ADR-019

  - id: E1-DMO1
    title: 三个演示镜像（PWM/UART 回显/AES-128）
    phase: phase-1
    deps: [E0-MAP5, E1-RUN3]
    est_days: 4
    deliverable: ethereal-images/ 三个镜像
    acceptance: 2 region 轮换运行；热替换 <10ms(BMC+DMA) / <100ms(上位机经 SPI)
    status: done   # 2026-09-13 对账: PWM 与 UART 两镜像闭环（pwm 64 向量全帧位真；uart 6873 周期 tick 位真 + 6 字节语义解码）已交付并经 TB 验证；AES-128 按既有结论递延至 Phase 2（C02 异构 S-box），属计划决定而非缺口；后续多镜像/热替换由 E1-DMO2/DMO2b/DMO2c 系列完成（57 ok/0 fail）

  - id: E1-DMO2
    title: 热替换稳定性测试（2x10000 次 + 干扰监测）
    phase: phase-1
    deps: [E1-DMO1]
    est_days: 2
    deliverable: tests/stress/ + 报告
    acceptance: 零配置损坏、零跨区干扰
    status: done   # 2026-09-11 (sim 范围): ① OCC 层字面 2×10000：tb_occ_soak 20000 次 BLANK→WRITE→READBACK（镜像 A/B 交替；v0.5 expected-CRC 门 + RAM 影子 + 邻区逐字比对）0 errors / 0 词失配（1.5 s wall）；② daemon 层轮换：tb_hotswap_stress 6 轮（STOP→run_packed→RUNNING，2 region × 签名镜像 A/B）**69 ok / 0 fail**，三层校验（EFP_ERR / RAM 影子 / 真实织物输出 + 邻区不受扰）；③ 工具：gen_daemon_vectors --frame-hex-b（第二签名镜像）+ `make stress` 独立目标。发现并记录：ALLOC 仅接受 FREE（热替换 = stop+deploy）、OCC_FRAME_ADDR 列字段 16 词步长 vs 34–68 词帧 → 同 region 多列别名（E1-DMO2b，spec v0.6 方案）。daemon 层字面 2×10000 = 硅范围（验签 ~0.84 s/轮）。report-E1-DMO2-hotswap-stress-20260911

  - id: E1-DMO2b
    title: OCC_FRAME_ADDR 列寻址修正（EMRI v0.6：col[11:8]/word[7:0]，256 词/列）
    phase: phase-1
    deps: [E1-DMO2]
    est_days: 1
    deliverable: spec v0.6 + daemon/tools 列基址迁移
    acceptance: 相邻列窗口不再别名（2 列部署地址不相交）
    status: done   # 2026-09-11 (维护者选方案 A): emri-v0.md v0.6 §2 重映射 {region[15:12],col[11:8],word[7:0]}；daemon.c 新增 column_frame_base(region,col)=region<<12|col<<8 并替换 4 处 per-column 调用（BLANK/WRITE/READBACK/heartbeat）；efp_client 增 region/column_frame_base + column_frame_range(>256 词拒绝) + packed 模型逐列 arming；frame_map.frame_addr 与 frame_addr_format 迁移到 v0.6；fw `-Werror` 干净 + 工具 pytest 406 passed（含 2 列地址不相交断言）。report-E1-DMO2b-frame-addr-v06-20260911

  - id: E1-DMO2c
    title: 多列 packed 部署端到端回放（≥2 列，真实 daemon 路径）
    phase: phase-1
    deps: [E1-DMO2b]
    est_days: 2
    deliverable: pack_tb_frames 多列镜像 + tb_bmc_daemon_packed 逐列流式/逐列 CRC 回读
    acceptance: 2 列部署 READBACK 逐列通过（v0.6 在真实 daemon 路径上无别名）
    status: done   # 2026-09-12: 2 列端到端达成 —— `tb_bmc_daemon_packed` **56 ok / 0 fail**（基线 25；本人独立复跑 exit 0 + 制作规则 PASS）：阶段 4 两列 run_packed（逐列 LOAD 握手 + 两列 READBACK CRC + WRITE 接受的 OCC_FRAME_ADDR 轨迹 == region<<12|col<<8 + 逐列 cfg 窗口校验 + col0 翻转/col1 恒 1）、阶段 5 签名篡改不 WRITE、阶段 6 stop 逐列 BLANK；工具链 `--frame-hex-cols` + `DAEMON_IMGCOL_*`；pytest 92 + lint 干净。途中修掉两个真实产品缺陷：① blank-before-write 脏位 per-REGION（见 f17721f，per-frame-window 位图 + §3.1 措辞修正）；② frame_decoder 捕获缓冲按 fbus_addr-frame_base_i 索引 → 基址为 0 时列≥1 的字被静默丢弃并复用陈旧缓冲（本 TB 已接正确基址；RTL 加固派给属主）。report-E1-DMO2c-multicolumn-20260912。**原发现的历史**： —— `occ_top` 的 blank-before-write 脏位原为 per-REGION（`frame_addr[15:12]`），而 v0.6 列映射把同一 packed 镜像的各列放在同一 region ⇒ col0 的 WRITE 弄脏 region 后 col1 的 WRITE 被拒（`NEEDS_BLANK`），`§3.1` 与 `§3.3`（逐列 BLANK→LOAD）在 cols>1 时自相矛盾（此前所有 packed TB 都是 IMG_COLS=1，故不可达）。已裁决并落地**方案 (a)**：脏位改为 per-frame-window 位图 `dirty_r[255:0]`（索引 `frame_addr[15:8]` = {region,col}，逐字流式置位/清零，region-wide 命令覆盖其触及的所有窗口，accept 时检查起始窗口），规范 §3.1 措辞同步为"per-frame-window dirty bit"；tb_blank 新增 packed 双列用例（`BLANK 0x0000 + 0x0100` → 两列 `WRITE` 均 DONE；未 BLANK 的活跃窗口仍 `NEEDS_BLANK` 且 RAM 不变；512 字 region-wide BLANK 清两窗口）。Dmo2cMulti 的双列 TB（帧生成/向量/TB 阶段）待其重跑验收后并入。E1-DMO1 的 4 列 uart 会话回放同属此项

  - id: E1-DMO3
    title: v0.1.0 里程碑发布（视频+博客+文档站）
    phase: phase-1
    deps: [E1-DMO2]
    est_days: 2
    deliverable: release v0.1.0 + 博客文章
    acceptance: 仓库公开，文档站上线
    status: blocked   # 2026-09-11: 依赖 E1-DMO2（sim 验收已过，见 report-E1-DMO2-hotswap-stress-20260911）；验收物全部为外部动作，需维护者执行。**2026-09-12 已把可本地完成的部分做完**：`docs/releases/v0.1.0-notes.md`（发布说明，逐条引用本仓库可复现证据）+ `docs/blog/2026-09-12-docker-for-fpgas.md`（博客初稿）+ `AGENTS.md` §7 状态块刷新（2026-09-12：当前队列、复现入口、证据映射、固件新鲜度陷阱、DMEM/Ed25519 栈预算警告）。⇒ 剩余 = 打 tag / 创建 Release / 8 仓库公开 / 演示视频 / 文档站上线 / 社区发布（外部动作）

  # ================= PHASE 2（中粒度） =================
  - id: E2-FAB1
    title: MEM-T tile（BSRAM 包装：位宽/双口/FIFO/ROM 预载）
    phase: phase-2
    deps: [E1-PLT1]
    est_days: 5
    deliverable: rtl/tiles/mem_t/
    acceptance: AES S-box MEM-T 版 vs LUT 版 eLUT 占用降 ≥5x
    status: done   # 2026-09-11 对账: 接受指标已达成 —— AES S-box 4779→290 eLUT + 16 MEM-T = 16.5×（≥5×）；tile RTL/frame_map/fabric_top/packed capstone 全链在位（report-P1-het-tiles/framemap/fabric-top/acceptance/packed-capstone 20260728-0730）

  - id: E2-FAB2
    title: DSP-T tile（27x18 MAC 模式封装）
    phase: phase-2
    deps: [E1-PLT1]
    est_days: 5
    deliverable: rtl/tiles/dsp_t/
    acceptance: FIR16 DSP-T 链实测 Fmax 与吞吐达标（vs LUT 版 ≥10x 吞吐）
    status: done   # 2026-09-11: ① 吞吐结构性达标（real-tap fir16 → 16 $macc_v2 级联 + 0 eLUT 数据通路；vs 124-eLUT LUT 版）；② **实测 Fmax（GW5AST-138，E1-PLT4 nextpnr 流程，post-route STA）**：单 MAC 93.77 MHz（cross-check 85.7）、整 dsp_t tile（config 驱动）69.35、fir16 16 级联 99.09 MHz（目标 50 MHz）；RTL↔综合网表等价 400 周期 0 diff、gowin_pack 出 34.7 MB 比特流；③ 新增实测：**开源 yosys/nextpnr 流程对 gw5a 不做 DSP 推断**（synth_gowin 的 mul2dsp 仅 gw1n/gw2a，dsp_map.v 无 27x18 模板）→ 上述数字为 LUT 映射下界，非 DSP 原语实现（器件 298 个 MULTALU27X18 全部未用）→ 原语映射缺口另立 E2-FAB2b。局部报告：docs/reports/report-E2-FAB2-fmax-20260912.md
  - id: E2-FAB2b
    title: GW5A DSP 原语映射缺口（开源流程 mul2dsp/dsp_map 无 27x18 模板）
    phase: phase-2
    deps: [E2-FAB2]
    est_days: 3
    deliverable: scratch mul2dsp 模板 + $__MUL27X18 → nextpnr MULTALU27X18 的强制映射，或 Gowin EDA 交叉核对
    acceptance: dsp_t tile 在真实 DSP 原语上的 post-route Fmax（并给出与 LUT 映射下界的差值）
    status: done   # 2026-09-12: **强制映射成功**（scratch `dsp_map_27x18.v` 模板 + coarse 流程，其余走既有 synth_gowin gw5a → nextpnr → gowin_pack）—— 单 27×18 MAC **1/298 MULTALU27X18，post-route 324.57/331.02 MHz**（LUT 基线 93.77 → **3.5×**；LUT4 2244→276、MUX2_LUT5-8 归零）；fir16 16 抽头级联 **16/298，259.34/258.33 MHz**（基线 99.09 → **2.6×**；LUT4 9042→867）；两者与 RTL 功能等价（iverilog，400/387 周期逐位一致）；`gowin_pack` 出 34.7 MB 比特流。**关键约束**：GW5A DSP 无符号端口（UG305E §2 全有符号；C=D=CASI=SIA=0 的 16 种模式均退化为 A×B）→ **有符号 $mul 可映射，全宽无符号 $mul 不得走此模板**（反例已证）。坑：`-run map_ram:` 续跑会缺厂商库并误报 "Module `ALU' ... not parametric"；照抄 techlib 默认参数字符串会让 apycula 写 UNKNOWN 属性 → 模板不携带参数。dsp_t **整 tile 补测完成**：2/298 MULTALU27X18，post-route **173.55 MHz**（LUT 基线 69.35 → 2.5×；LUT4 5506→284；2 个 DSP 正确——registered multiply 与 p0=a*b+c bypass 均存活）。等价性：DSP 网表 vs LUT 网表 0/401 逐位一致；原始探针 RTL↔网表 356/401 不一致与映射无关且先于本任务（`dsp_t.mode_r` 无复位 → RTL 从 x 起、GW5A 模型带 INIT=0；配置自周期 0 有定义的探针 probe_dsp_t2 → RTL vs DSP 0/401）。用量/Fmax 取自 nextpnr P&R 日志表（基线校正为 MAC 2327 / FIR16 9551）。scratch 在 generated/fab2b_dsp/（gitignored），未改任何 tracked 文件

  - id: E2-FAB3
    title: SSM-T tile + 上下文保存 v1（FF 状态扫描读/写，BMC 编排）
    phase: phase-2
    deps: [E1-RUN2]
    est_days: 5
    deliverable: rtl/tiles/ssm_t/ + context save/restore
    acceptance: 容器暂停/恢复演示通过
    status: done   # 2026-09-11 对账: 暂停/恢复演示通过 —— ctx-scan-v0 扫描链（elut4/clb_t/fabric_top + occ/ctx_scan 引擎）+ tb_ctx_scan 位精确续跑比对 24 周期（report-E2-FAB3-ctx-scan-20260911）；BMC/EMRI 编排拆至 E2-FAB3b
  - id: E2-FAB3b
    title: 上下文保存 v1 编排（EMRI CTX_* 寄存器 + BMC 发起暂停/恢复 + 多字链扩展 TB）
    phase: phase-2
    deps: [E2-FAB3]
    est_days: 3
    deliverable: emri CTX_* register block + bmc-fw context orchestration
    acceptance: BMC 驱动的容器暂停/恢复（多字链 ≥2 字）位精确
    status: done   # 2026-09-12: EMRI v0.7 §3.9 冻结 + 四面落地 —— ① RTL: ctx_engine_wrap（新，持有 ctx_scan + PAUSED 冻结，终结周期桥接/释放）+ emri_regfile CTX 面（无引擎/织物/窗口）+ **ctx_scan 多字链 restore 字序修复**（原仅 W=1 正确，tb_ctx_multiword 抓出，变异双证）；② 固件: ctx_chain_words()（EMRI 几何推导）、ctx_issue()（有界轮询）、do_ctx_save/restore、stop/abort 先解冻再 BLANK、PAUSED restart 拒绝；③ 主机: emri_constants v0.7 + ABI 配对；④ 证据: lint/formal 3 证/test-model 2724+2xfail/tb_ctx_multiword(2 字链位精确)+tb_emri_regfile+tb_mon_anomaly+tb_ctx_scan(W=1 回归) PASS/**tb_bmc_daemon 83 ok 0 FAIL**（阶段 1c: RUNNING→PAUSED(9)+512 周期冻结→RUNNING(6)+恢复翻转 + 两个 ctx_error 负例）。report-E2-FAB3b-ctx-orchestration-20260912。**遗留**：同一测试内"BMC + ≥2 字链"需 ≥2 列/区构建（区域↔列映射参数化）

  - id: E2-FAB4
    title: fabric.yaml v2（异构 region 组合 + supertile）
    phase: phase-2
    deps: [E2-FAB1, E2-FAB2]
    est_days: 5
    deliverable: fabric-gen v2 + 帧映射 v2
    acceptance: 混合 tile region 生成/配置/运行全链路通过
    status: done   # 2026-09-11 对账: 混合 tile 全链路已证 —— fabric_2x2_het 描述符 + TILE_LAYOUT/frame_map het + fabric_top TILE_TYPE + vbus↔虚拟路由双向打通（tb_vbus_route）+ 异构 packed capstone（解复用 backdoor 探针 + CLB 翻转）；report-P1-het-framemap/fabric-top/vbus-routing/het-packed-capstone（20260728-0730）

  - id: E2-FAB5
    title: 互联优化 v2（两源轨道 + LUT 平台期对齐）
    phase: phase-2
    deps: [E1-PLT1]
    est_days: 5
    deliverable: rtl/interconnect v2
    acceptance: 同规模 fabric 物理 LUT 降 ≥20%
    status: done   # 2026-09-02: v2c 实装完成（spec §7 FROZEN → RTL + 模型 + frame_map + bitgen + VPR arch + 全部 TB/生成器迁移）；实测真实 RTL 4×4 = 29,869 LUT = 233:1 = **-24.3%**（≥20% 达标，yosys synth_gowin 同口径）；c432 全帧路径 32/32 bit-true，present_round 翻转 xfail→PASS；reports: v2c-rtl/v2c-tools-20260902

  - id: E2-MAP1
    title: 异构映射（Yosys mem/dsp 推断 + VPR 异构 arch + bitgen v2）
    phase: phase-2
    deps: [E2-FAB4]
    est_days: 8
    deliverable: tools/mapper v2
    acceptance: 含 RAM/DSP 的基准端到端 bit-true
    status: done   # 2026-09-11 对账: 交付物与验收均在位 —— synth_ethereal.py（异构 Yosys flow: memory -nomap + alumacc）在 arch_ethereal.xml（+mult_27x18/+mem_2Kx32 tiles）上推断 RAM→$mem_v2 / mult→$macc_v2，既有基准仍路由（c432 CPD 5.30ns / 188.7MHz）（report-P1-het-map-infra-20260728）；端到端 bit-true 由异构链兑现：fir16 real-tap → 16 $macc_v2 级联 0 eLUT 数据通路、AES S-box 4779→290 eLUT+16 MEM-T（report-P1-het-acceptance-20260728）+ het frame_map/fabric_top/packed capstone TB（shell_tb_het_packed PASS，本轮亦复跑）

  - id: E2-IO1
    title: L2 代理库扩展（SPI/I2C master/PWM/QEI）+ RFC-004 冻结
    phase: phase-2
    deps: [E1-IO4]
    est_days: 6
    deliverable: rtl/io/proxy_*/ + spec RFC-004
    acceptance: 虚拟设备寄存器规范 v1.0 发布
    status: todo

  - id: E2-IO2
    title: L1 mux v2 + 引脚时序表征
    phase: phase-2
    deps: [E1-IO3]
    est_days: 3
    deliverable: pinmux v2 + manifest 更新
    acceptance: 8 组引脚池；各路 max 频率实测写入 manifest
    status: todo

  - id: E2-BMC1
    title: mFSM 精简管理单元（无 CPU 寄存器式，Profile-E）
    phase: phase-2
    deps: [E1-BMC4]
    est_days: 4
    deliverable: rtl/mfsm/
    acceptance: 仿真中上位机脚本经 EMRI 驱动完整部署流程；ethctl 无感切换
    status: done   # 2026-09-13 收官: mFSM + EBI-Tiny（含 E0-SHL1）交付。新增 `rtl/ebi/{ebi_pkg,ebi_tiny}.sv`（单主 32 位 valid/ready 寄存器总线 + 组合地址映射 decoder；6 窗口；未解码/区域越界一律 ready+err 绝不挂死）、`rtl/mfsm/{mfsm_pkg,mfsm_session,mfsm_top}.sv`（**复用 `emri_regfile #(HAS_BMC=0)` 原样**，ADR-015 的"同一 ABI"由构造保证；§5 五状态会话 FSM）。验收证据：E0-SHL1 的 BFM 随机一致性 **TEST PASSED（13,944 checks / 10,000 ops，各窗口提交数==写入数，6 个额外种子全过，Verilator 下同数通过）**；E2-BMC1 的部署流程 **TEST PASSED（320 checks）**：主机→EBI-Tiny→mFSM→OCC→真实 fabric_top 的完整 BLANK→begin_rx→12 字流→VERIFY_REQ→verify→occ_go→OCC DONE→ADR-019 CRC 门控 READBACK→TFF 翻转 + 镜像 B 热替换，另含 abort/无操作/LOCKED/CRC/NEEDS_BLANK 角落与五状态覆盖；**ABI parity sweep：0x00-0x3F + 0x50-0x5F 与 BMC 模式同 OCC 输入逐项比对，仅 `CAPABILITIES`（has_bmc 0/1）一处不同**。金模型 45 pytest，`make test-model` 2831 passed；verilator -Wall 三个 unit 全 0 警告无豁免；Makefile 已接线（RTL_CLEAN + deps + 两条 test-sv）。8 条假设按 G6 以 `// ASSUMPTION (TBD, 2026-09-13)` 落在代码里，规范歧义清单见报告

  - id: E2-BMC2
    title: （可选）VexRiscv 备选核 wrapper 验证
    phase: phase-2
    deps: [E1-BMC2]
    est_days: 3
    deliverable: bmc_core/vexriscv 变体
    acceptance: 同一固件 ABI 在备选核跑通，验证核可替换性
    status: todo

  - id: E2-PLT1
    title: Zynq US+ 移植（hal/xilinx_us + BMC 同款软核 + Linux 上位机）
    phase: phase-2
    deps: [E2-FAB4, E1-RUN2]
    est_days: 8
    deliverable: hal/xilinx_us/ + runtime/linux/
    acceptance: Phase 1 全部演示在 US+ 复现；同一 image 双平台直接运行
    status: todo

  - id: E2-PLT2
    title: 原生 DFX 槽位并存（DFX Controller 接入生命周期）
    phase: phase-2
    deps: [E2-PLT1]
    est_days: 6
    deliverable: runtime/dfx/
    acceptance: overlay 容器 + 原生 DFX 加速核混合运行演示
    status: todo

  - id: E2-SEC1
    title: 安全 v1（强制验签 + region 锁矩阵 + 能力清单校验）
    phase: phase-2
    deps: [E1-RUN2]
    est_days: 4
    deliverable: runtime/security/
    acceptance: 未签名镜像拒绝；越权 IO 分配拒绝
    status: done   # 2026-09-11: ① 强制验签默认开启 —— 固件侧本就无条件 Ed25519；主机 EFP 路径此前**无**验签，现默认强制（efp_client.load_image(trusted_pk) / ethctl --pubkey），未签名与坏签名均拒绝（pytest + 既有 bad_sig TB）；② 能力清单校验 —— 新规范 ethereal-spec/security/capabilities-v0.md（带类型条目）+ ethereal-runtime/security/capcheck.py + ethimg/efp_client/ethctl 集成（越权 IO/未知服务拒绝、条目级报错）；设备侧 EMRI v0.6 §3.7 能力门（CAP_DECL_IO/SVC + CAP_STATUS）由 daemon 在 ALLOC 后/BLANK 前执行（EFP_ERR=11 + 事件 code=4，拒绝无副作用）；③ 异常监控 v1 —— EMRI v0.6 §3.8 实时窗口监控（逐区饱和计数/阈值/spike 标志/throttle 掩码/code=5 事件，环 push 仲裁在 regfile 内）+ daemon throttle 执行（EFP_ERR=12）+ MON_NOTIFY 通知点。证据：make lint OK / make test-model 2724 passed + 2 xfailed / fw `-Wall -Wextra -Werror` 干净 / tb_bmc_daemon 61 ok + tb_bmc_daemon_packed 25 ok（均 TEST PASSED）/ tb_mon_anomaly（新）+ 6 个 iverilog TB PASS。report-E2-SEC1-security-v1-20260911。**未含**：region 锁矩阵（现仅单 region_locked_i 门 + occ_region_locked_o 硬连线 0）→ E2-SEC1b
  - id: E2-SEC1b
    title: region 锁矩阵（LOCK_MATRIX 位图 + LKM_STATUS/LKM_CMD，S02 §2.3 / C03 §5）
    phase: phase-2
    deps: [E2-SEC1]
    est_days: 3
    deliverable: occ_top 逐区锁位 + EMRI LKM 寄存器面 + daemon 锁生命周期联动
    acceptance: 越权 region 写被拒且事件上报（S10 §3 Phase-1 #1）
    status: done   # 2026-09-12: EMRI v0.8 §3.10 + occ_top 逐区硬件写门（region_locks_i[7:0]+global_lock_i；WRITE/BLANK 拒 → done_code=LOCKED(3) → daemon EFP_ERR=3；READBACK 不受限）+ emri_regfile LKM 面 + daemon 生命周期（RUNNING 锁、BLANK 前解锁、PAUSED restart 拒绝）。端到端 BMC TB 抓出两个真问题并修掉：① LKM_CMD=4 曾连逐区锁一起清 → 规范澄清"仅清全局位"（LKM_OP_GLOBAL_CLEAR 只动 global_lock_r）；② 既有握手死锁 —— occ_top 在 LOCKED/NEEDS_BLANK 拒绝时不给 cmd_ready，单宿主端口永久挂住 → regfile 于该状态释放挂起事务（NEEDS_BLANK→occ_reject 亦在寄存器 ABI 上重新可达）。证据：tb_lock_matrix（新）+ 22 个 ripple TB + tb_bmc_fw/tb_ethctl_replay（真固件）+ tb_occ_soak 20000 零损坏 + formal 全过（含 emri_r_occ_decode）+ make lint 干净；3 项负控按预期失败。report-E2-SEC1b-lock-matrix-20260912。注：修正后的 tb_bmc_daemon 阶段 1d 复跑确认中（首轮已证 RUNNING 自动锁/裸写被拒且 RAM 逐字节不变/全局锁闸门）

  - id: E2-AST1
    title: Astral 聚合 v1（Zephyr+WAMR + Type-F 容器经 EFP 调 vFPGA）
    phase: phase-2
    deps: [E1-RUN3]
    est_days: 8
    deliverable: astral-os/ 最小运行时 + 聚合 demo
    acceptance: WASM 容器与 vFPGA 逻辑双向数据通路演示
    status: blocked   # 2026-09-12 环境核查: `astral-os/` 仅为脚手架（README/LICENSE），本机**无 Zephyr/west/WAMR/wasmtime** → 需外部 SDK 与多 GB 依赖，属环境门控。**2026-09-12 已按 phase-2 §3 熔断条款交付降级演示「MCU 脚本部署」**：Flash 常驻脚本格式（CRC32 门 + 9 操作码 + 硬预算）+ 无 malloc 解释器（daemon 扩展钩子 `EFP_CMD_SCRIPT=10`，生产固件仍回 `bad_cmd` ⇒ 无需 spec 变更）+ 生成器 + `tb_bmc_script`（真实链路：启动→CRC 门→DEPLOY→**Ed25519 验签**→区域 0 RUNNING→LOOP×2→STOPPED；外加 CRC 损坏/签名篡改两条拒绝路径）⇒ **TEST PASSED，0 FAIL**，`tb_bmc_fw` 回归 PASSED；报告 `docs/reports/report-E2-AST1-mcu-script-deploy-20260912.md`；发现 DMEM 增长侵蚀 Ed25519 验签栈（移交 E2-BMC）。⇒ **剩余为 WASM 组件验收**（容器与 vFPGA 双向数据通路），仍待运行时环境

  - id: E2-DOC1
    title: 规范冻结（EBI/镜像/BoardManifest/EFP v1.0）
    phase: phase-2
    deps: [E2-IO1, E2-PLT1]
    est_days: 3
    deliverable: ethereal-spec/ v1.0 tags
    acceptance: 四份规范语义化版本发布
    status: todo

  # ---- ADR-018 执行线（2026-07-30 决议：全自研 AXI + eth_rv RV64 + wrapped DRAM + 自研 DMA）----
  - id: E2-AXI1
    title: 自研 AXI fabric（eth_axi：Lite/Stream/xbar + skidbuf + wb2axi，形式化验证）
    phase: phase-2
    deps: []
    est_days: 30
    deliverable: ethereal-shell/rtl/axi/ + formal/*.sby
    acceptance: AXI4-Lite/Stream/xbar 过 SymbiYosys 性质证明；iverilog/verilator 双仿真兼容
    status: done   # 2026-08-30: 4 项 k-induction 证明 PASS（make formal）；见 report-P1-formal-*-20260830

  - id: E2-RV0
    title: eth_rv DiffTest 锁步验证载体（Spike 构建 + commit-trace 比对 + RV64IMC 测试语料）
    phase: phase-2
    deps: [E2-AXI1]
    est_days: 5
    deliverable: ethereal-shell/verif/eth_rv/（Spike 构建于 generated/ + rv_difftest CLI + DUT 适配器 + ≥3 个 RV64IMC bare-metal ELF + pytest）
    acceptance: 零分歧 PASS/注入分歧定位首个 pc/cycle FAIL 双路径可复现
    status: done   # 2026-09-12: 载体落地 `ethereal-shell/verif/eth_rv/`（README + rv_trace/rv_image/rv_spike/rv_dut/rv_model/rv_difftest + corpus 4 个自检 RV64IMC ELF + 121 pytest）。Spike 1.1.1-dev 构建于 generated/（commit 1e05ddac）+ dtc 1.8.1（Spike 运行时 shell-out，缺则无法启动 ELF）。验收双路径：corpus 4 程序 **MATCH 221/169/105/116 commits，0 分歧 exit 0**；`--inject 12:value=0xdeadbeef` → 精确定位首个分歧（pc/cycle/rd）exit 1；畸形输入 exit 2。`make verif-rv` 已加入（corpus 构建 + 121 passed）。差分环自身抓出 5 个真实缺陷（C.J imm bit5、mulh 高半字、c.addi16sp 位映射、hex 字节序、语料自检）→ 工具有效性证据。遗留增量：内存访问比对（Spike mem 日志已在 Commit 留字段）、CSR/trap 流（RV-C 需要）、实时锁步 stepper

  - id: E2-RV1
    title: eth_rv Stage RV-B（RV64IMC 顺序 5-6 级，bare-metal/FreeRTOS，DiffTest vs Spike）
    phase: phase-2
    deps: [E2-AXI1]
    est_days: 180
    deliverable: ethereal-shell/rtl/eth_rv/ + AXI4 socket wrapper + core.yaml 描述符
    acceptance: spike 锁步比对零分歧跑 bare-metal hello；iverilog/verilator 可仿真
    status: done   # 2026-09-12 增量 1-3 达成（切片 → 内存流 DiffTest+CSR/trap → 形式化轨迹契约+AXI4 主口）：**增量 1** `ethereal-shell/rtl/eth_rv/`（5 级顺序核）对 Spike 4/4 语料 MATCH；**增量 2** 轨迹 `cycle pc rd value [mem_addr mem_wdata [masks]]` + M 模式 CSR/trap（10 CSR、ecall/ebreak/非法/非对齐，位精确）+ 语料 cor_csr/cor_trap ⇒ **`make verif-rv` 147 passed、6/6 程序 MATCH（memory active）**；**增量 3** `eth_rv_core.sby`（RVFI 轨迹契约 P1-P6/P3b，k-induction PASS + 11/11 cover）+ `eth_rv_axi_master.sv`（D 口 AXI4 INCR，存储 B 后才应答、单拍/行填充读 + 1 行失效缓存）⇒ **`run_difftest.py --dram` 6/6 MATCH（29 次多拍突发、5 次缓存命中）**，负控仍精确捕获；lint/ruff/mypy 干净。report-E2-RV1-rv-b-slice-20260912（§4/§6）。**增量 4** `eth_rv_uart.sv`（16550 子集，LSR/IIR/MSR 镜像 Spike ns16550 ⇒ THRE 轮询退役一致）+ `eth_rv_mmio_mux.sv`（设备地址从内存路径门掉、内存直通）+ `cor_hello.S` ⇒ **`hello, eth_rv!` 15 字节、15 帧 × 160 周期、0 帧错**，**7/7 程序在拍与 AXI/DRAM 两条路径均 MATCH**（cor_hello AXI 侧仅 1 AR/1 AW ⇒ UART 存取从不触达插座），`make verif-rv` **176 passed**。**增量 5** D 口/取指口 access-fault 陷阱（**先以 Spike 为裁判**确认 13 组语义，并修正增量 4 的假设：Spike 对 UART 页内"未实现偏移"**不**陷阱 —— ns16550 `addr &= 7` 别名，唯一页内陷阱是**非字节宽度**）+ `cor_fault.S`（11 个陷阱、程序内断言 `mcause/mtval/mepc`）⇒ **8/8 程序 MATCH（拍/AXI-DRAM/--memlat 2/--axi-line-beats 1 四路）**，`make verif-rv` **181 passed**；负控 `+starve_dmem=1`（旧卡死）与 `+no_dmem_err=1`（静默成功被 DiffTest 在 commit #20 抓到）均生效。⇒ **C14 §8 六个检查点全部 ✅**。**剩余（RV-B 尾 / RV-C 前置）**：I 口 AXI 取指、缓存/BTB、中断与特权级切换、`mip` 值比对（Spike 报 MTIP）、实时锁步 stepper；RV-C（FPU + Sv39 → Linux）为后续里程碑：`ethereal-shell/rtl/eth_rv/`（pkg + ALU/乘除/LSU/寄存器堆/译码 + 顶层 5 级顺序核，EX/MEM/WB→EX 前递 + load-use 气泡）→ 运行器对 Spike **4/4 语料 MATCH、0 分歧**（221/169/105/116 提交，exit 0；`--memlat 2` 仍全绿）；RVFI 轨迹口为一等接口；负控（value/pc/rd 注入）均在精确提交处 FAIL；差分环抓出并修掉 2 个真实 RTL 缺陷（写优先旁路、D 口 lane 约定）；错误不静默（ecall/非对齐 → 错误脉冲）。Makefile 增 `verif-rv-rtl` 目标 + RTL_CLEAN/deps（包参数按惯例 -Wno-UNUSEDPARAM 豁免）。report-E2-RV1-rv-b-slice-20260912。**剩余**：CSR/trap 最小集、mem 流与 Spike 比对、`eth_axi` AXI4 主口替换（当前 I 口为简化取指）、缓存/BTB、轨迹口形式化、RV-C（FPU+Sv39）；**增量 4** UART `hello, eth_rv!`（16550 子集、LSR/IIR/MSR 镜像 Spike ⇒ THRE 轮询退役一致）+ MMIO mux ⇒ 15 帧×160 周期、0 帧错，**C14 §8 六检查点全 ✅**；**增量 5** D 口/取指口 access-fault 陷阱（先以 Spike 探针确认 13 组语义，修正增量 4 假设：UART 页 `addr &= 7` 别名、唯陷阱是非字节宽度）+ `cor_fault` ⇒ **8/8 MATCH**，负控覆盖卡死与静默成功；**增量 6** M/S/U 特权 + `mret`/`sret` + 各模式 `ecall` + `medeleg`/`mideleg` 委托 + S 模式 CSR 合法性 + **CLINT**（MSIP/MTIMECMP/MTIME，`mtime` 对齐 Spike boot-ROM 预载 ⇒ 时间可比对）+ 中断优先级（MEI>MSI>MTI>SEI>SSI>STI）+ 语料 `cor_priv`(604)/`cor_deleg`(244)/`cor_intr`(491)/`cor_time`(6517) ⇒ **12/12 程序 MATCH（拍 + AXI/DRAM 双路径）**，`make verif-rv` 181 passed，`eth_rv_core.sby` prove（k-induction）+ cover 均 PASS（本机复跑）。**剩余**：RV-B 尾（缓存/BTB）、UART RX、可选 uart 证明 → 再进 RV-C（FPU + Sv39 → Linux）　⇒ **2026-09-12 验收达成，本任务 done**：C14 §8 六检查点全 ✅ + 增量 4/5/6（UART hello、access-fault 陷阱、M/S/U 特权与中断）；`make verif-rv` 181 passed、**12/12 语料对 Spike 零分歧（拍 + AXI/DRAM 双路径）**、`eth_rv_core.sby` prove/cover PASS、lint 干净、裸机 hello 经真实 8N1 UART 输出（15 帧 × 160 周期、0 帧错）。后续深度（F/D、Sv39、Linux 启动）转入 **E2-RV2（RV-C）**；缓存/BTB 属性能项，验收不要求（且会改变内存可见性、破坏当前逐访问比对契约，需先设计"可 diffable 的缓存"再动）。

  - id: E2-RV2
    title: eth_rv Stage RV-C（RV64GC + Sv39 MMU → GW5 单核验证 profile，boot Linux）
    phase: phase-2
    deps: [E2-RV1]
    est_days: 180
    status: done   # **2026-09-12 收官：RV-C 里程碑达成 —— RTL 上 boot Linux**：`run_linux_boot.py --rtl` 在拍 D 口构建上 **TEST PASS 362,867,491 commits / 2,340,426,625 cycles**，17,558 陷阱（6,619 中断、0 access fault、**84 page faults** ⇒ 内核 A/D 路径确实走到），UART **11,968 字节 / 11,968 帧 / 0 帧错**，控制台出现 `eth_rv S4: MILESTONE-REACHED`，且与 Spike 控制台**逐字节相同**（仅时间戳数字与一处实测比值差异，结论一致）；Phase 1（Spike）2.3 s 到达。过程中修好 6 处真实 RTL 缺陷（EX 操作数冒险、`sc` 保留集过早清除、多周期停顿操作数陈旧、`mcounteren` 误用于 M 模式、抑制位采样时机、S 资格缺 M 子句）+ 2 项缺失特性（`mstatus.MPRV`、PMP CSR 地板）+ 3 项 DT/框架契约（PMP 区域数须由 DT 声明、mtime 节拍按构建档分离、timebase 为仿真尺度并注明）。当前瓶颈属性能项而非缺陷：无 TLB/PTE 缓存/I-cache ⇒ 6.45 cycles/instruction（IPC 0.155），即差距清单 G10；下一步 = TLB + 小 I-cache（并让 `sfence.vma` 落地）+ `/chosen/rng-seed` 省掉 ~50M 指令熵阶段。C14 的 RV-C 检查点表列为后续文档项。原注：
    deliverable: eth_rv MMU/FPU 扩展 + OpenSBI/Linux 启动链
    acceptance: GW5AST-138 仿真/实板 boot Linux（DDR-less rootfs 允许）

  - id: E2-DMA1
    title: eth_dma_mc 多通道 scatter-gather DMA（AXI4 主，BMC 热替换帧 DMA 复用）
    phase: phase-2
    deps: [E2-AXI1]
    est_days: 45
    deliverable: ethereal-shell/rtl/dma/eth_dma_mc.sv + formal
    acceptance: 描述符链表 SG 传输 TB 通过；形式化 VALID/READY 性质
    status: done   # 2026-09-12 (垂直切片): `ethereal-shell/rtl/dma/`（eth_dma_pkg 描述符布局/CSR 映射/错误码 + fifo + arb + **axi_engine 单未完成 AXI4 INCR 主**（6 态 FSM + FORMAL 性质块）+ channel（取/译/校验/突发分块/CSR/IRQ）+ top（CSR 文件/N 通道/仲裁/ID 路由/聚合 irq）+ AXI4 主口）。描述符 32 B（SRC/DST/NXT/LEN+SOF/EOF/IOC，32 B 对齐，一次 INCR 读回）；v0 规则与分块策略见头注释。**TB `tb_eth_dma_mc` 60 checks / 0 errors**（DUT=顶层，内存=真实 `eth_dram_ctrl` 插座）：多描述符链校验和、双通道交织、两条 DECERR 错误路径不挂死、LFSR 背压、连续 AXI 协议监视器（VALID 不撤回/WLAST/单未完成/beat 计数）。**形式化 `eth_dma_mc.sby` k-induction PASS + cover**（已并入 make formal）。非目标：多未完成/Stream/2D/状态回写/循环链/LEN>65535；CSR 为通用 valid/ready（AXI4-Lite 外壳属集成层）；与 xbar 的挂接需 E2-AXI2 突发扩展。report-E2-DMA1-dma-mc-20260912

  - id: E2-DMA2
    title: eth_dma_2d 图形 DMA（ND-strided + blit/fill/ROP，Service-Tile 级）
    phase: phase-2
    deps: [E2-DMA1]
    est_days: 45
    deliverable: ethereal-shell/rtl/dma/eth_dma_2d.sv
    acceptance: 2D block move + blit/fill TB 通过（VDMA 风格寄存器模型）
    status: todo

  - id: E2-AXI2
    title: eth_axi_xbar v0.1 burst 扩展（AxLEN/AxSIZE/AxBURST/WLAST/RLAST 贯通）
    phase: phase-2
    deps: [E2-AXI1]
    est_days: 8
    deliverable: eth_axi_xbar v0.1（beat 计数 + 突发通道端口）+ spec eth-axi-v0.md §5.2 修订 + TB
    acceptance: 突发事务经 xbar 到 eth_dram_ctrl 全链一致（对比直连基线）
    status: done   # 2026-09-12: 新参数 `BURST_EN`（默认 0 = v0 单拍契约**逐位不变**；1 = 突发路由）+ 主/从侧 burst 端口；突发形状随 skid 载荷；每主 beat 计数（末拍 = LAST | cnt==AxLEN，与 DRAM stub 容错一致 ⇒ 不挂死/不截断）；锁步跨整突发（wr_busy 到 B、rd_busy 到 LAST R）⇒ (U)/(C)/(O) 不变式中途仍成立；DECERR 吞整突发 W 且只回 1 个 R 拍。证据：新 `tb_axi_xbar_burst` **2813 checks/0 errors**（256 拍 sweep 与直连基线一致、双主并发突发、3 项非空洞变异全 FAIL）；形式化 prove PASS（40 步 + k-induction，6m29）+ cover 11/11；既有 `tb_axi_xbar`/`tb_bmc_axi_xbar` 回归 PASS；lint 0 告警（含 -GBURST_EN=1）。spec §5.2 修订文本见 axi2-notes.md §4（agent 笔记，父侧并入）。report-E2-AXI2-xbar-burst-20260912

  - id: E2-DRAM1
    title: eth_dram_ctrl 封装件（AXI4 从接口 + Zynq-PS/GW5-hard-DDR3 glue + Verilator stub）
    phase: phase-2
    deps: [E2-AXI1]
    est_days: 20
    deliverable: ethereal-shell/rtl/dram/ + hal/*/glue/
    acceptance: 行为 stub 上全 SoC 可仿真；GW5 glue 留待实板 bring-up
    status: done   # 2026-09-12 (sim 范围): 交付 `ethereal-shell/rtl/dram/{eth_dram_ctrl.sv（AXI4 从插座+seam）, eth_dram_stub.sv（完整行为级 AXI4 内存从机）}` + `ethereal-fabric/hal/{gowin_gw5,zynq}/glue/eth_dram_glue.sv`（每目标一实现、构建期选择；真实 PHY 留 bring-up）+ `tb_eth_dram_ctrl.sv`。证据：6× verilator --lint-only -Wall 零告警；TB iverilog/verilator 双跑 **13854/13492 checks、0 errors、800 笔 soak tx** + TEST PASSED（vendor seam 两个方向同结果）；**4 项变异全部 FAIL**（丢首写拍/重复读拍/忽略 WSTRB/禁用 DECERR）；`make lint` 已纳入 RTL_CLEAN。已知集成缺口：v0 xbar 无突发端口 → 见 E2-AXI2；MEM_BASE=0x8000_0000 为 ASSUMPTION（SoC 内存映射未冻结，参数可配）。report-E2-DRAM1-dram-ctrl-20260912

  # ================= PHASE 3+（里程碑级） =================
  - id: E3-SVC1
    title: NPU-Tiny Service Tile（INT8 8x8 systolic + TinyML demo）
    phase: phase-3
    deps: [E2-FAB4]
    est_days: 15
    deliverable: service tiles + 推理 demo
    acceptance: 吞吐/能效报告；多容器分时复用无状态泄漏
    status: todo

  - id: E3-SCH1
    title: 配置调度器（镜像分组 + 预取 + 缓存，BMC 执行）
    phase: phase-3
    deps: [E2-PLT1]
    est_days: 8
    deliverable: bmc-fw/scheduler/
    acceptance: 预取命中冷启动 P50 <100ms
    status: todo

  - id: E3-SCH2
    title: 容器迁移/抢占 + 碎片整理 v1
    phase: phase-3
    deps: [E2-FAB3]
    est_days: 10
    deliverable: bmc-fw/migration/
    acceptance: region 间迁移演示通过
    status: todo

  - id: E3-SEC2
    title: 安全 v2（虚拟配置静态检查钩子 + 异常功耗/时序监控）
    phase: phase-3
    deps: [E2-SEC1]
    est_days: 8
    deliverable: runtime/security/v2/
    acceptance: 环振/短路结构扫描检出率报告
    status: todo

  - id: E3-REP1
    title: OCI 兼容镜像仓库 v1（ethctl push/pull）
    phase: phase-3
    deps: [E1-RUN1]
    est_days: 5
    deliverable: tools/registry/
    acceptance: push/pull/版本管理可用
    status: done   # 2026-09-12: `ethereal-tools/tools/oci_registry.py`（LayoutStore + 最小 RegistryClient，Store Protocol 抽象；`python -m oci_registry` CLI）+ 48 pytest + `ethctl push/pull` 子命令（S08/S09 承诺的命令面）。映射：layer = `.eth` tar（`application/vnd.ethereal.image.v1+tar`）；config = 清单摘要（`…config.v1+json`，含 manifest_digest/成员摘要/签名指纹/artifact 描述符，并设 artifactType）；tag = index 条目（`ref.name` + `io.ethereal.image.name`）。信任：签名仍由 `ethimg.verify` 校验（pull 时按调用方策略，默认 strict）——无 OCI 专属信任通路。完整性顺序：blob 摘要+尺寸 → config↔layer 匹配 → tar 成员摘要 + manifest_digest → 身份/指纹匹配；目标经 `.part` + `os.replace` 仅在全部通过后落地。证据：48+65 pytest、ruff 干净；假 registry（线程内 HTTP）往返/404/篡改/传输错误；篡改用例覆盖 layer/manifest/全量重摘要图/嫁接 config/身份谎言。边界：RegistryClient 最小化（真实 GHCR/Harbor 还需 token 握手、分块上传、cross-repo mount、referrers、TLS/重试、skopeo/oras 互操作 —— 见 notes）；v1 假设每镜像恰好 1 个 .eth layer（第二个 layer 报错不忽略）

  - id: E3-PUB1
    title: 论文投稿（FPL/FCCM/TRETS）
    phase: phase-3
    deps: [E2-FAB5, E2-PLT1]
    est_days: 15
    deliverable: 论文草稿
    acceptance: 投稿完成
    status: todo

  - id: E3-MON1
    title: 监控 v2（Zynq SEM 擦洗由 BMC 调度；I2C 命令集扩展）
    phase: phase-3
    deps: [E2-PLT1]
    est_days: 6
    deliverable: bmc-fw/scrub/ + monitor v2
    acceptance: SEU 注入实验自动修复演示
    status: todo

  - id: E4-ORC1
    title: 统一编排器（bundle/多板/声明式 YAML）
    phase: phase-4
    deps: [E3-SCH1]
    est_days: 20
    deliverable: ethereal-orchestrator/
    acceptance: 多板部署 demo
    status: todo

  - id: E4-BMC1
    title: BMC 固件迁移 Zephyr → BMC 成为 Astral 节点
    phase: phase-4
    deps: [E3-SCH1]
    est_days: 10
    deliverable: bmc-fw/zephyr/
    acceptance: Astral 运行时在 BMC 上运行，Type-F 容器本机部署
    status: todo

  - id: E4-DEV1
    title: 开发者体验（VS Code 扩展 + TUI 仪表盘）
    phase: phase-4
    deps: [E3-REP1]
    est_days: 15
    deliverable: tools/ide/
    acceptance: 镜像构建/部署/调试 IDE 内闭环
    status: todo

  - id: E4-KIT1
    title: 参考设计套件 x4（电机控制/SDR/工业网关/NPU 协处理）
    phase: phase-4
    deps: [E3-SVC1]
    est_days: 25
    deliverable: kits/
    acceptance: 每个套件含完整文档与演示
    status: todo
