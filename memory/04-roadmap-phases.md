# Roadmap & Phases — Ethereal Platform

> Solo maintainer, ~8-15h/week. Each phase has circuit-breaker (防烂尾 key design). Task statuses live in `docs/ethereal-tasks.yaml` (2026-09-01: Phase 0 closed; P1 in flight).
> Synced from `/memories/repo/roadmap-phases.md`.

## Progress log (2026-09-01 — Phase-0 closeout + P1 daemon wave)
- **Phase-0 formally closed (report-phase0-closeout-20260901).** Decisions: **D1** EBI-Tiny (E0-SHL1) superseded for the P0 shell by the ADR-018 AXI path, demoted to small-device fallback landing with E2-BMC1; E0-SHL2 done (tb_mgmt_hotswap + tb_bmc_axi_fabric). **D2** E0-MAP5 partial acceptance ratified (pwm/crc32 bit-true; fir16/aes resolved via het DSP/MEM-T; present_round xfail = known v1.1 boundary → E2-FAB4 input). **D5** mailbox B-channel = intentional posted-write (spec §2.6 fire-and-forget), NOT an omission; `rtl/mailbox/` 10 files now `-Wall` zero-warning (20k-cycle old-vs-new equivalence EQUIV-PASS ×2) → graduates to main lint gate; `interface/` keeps 2 documented design warnings (BLKSEQ spi_sat:143; **MULTIDRIVEN uart_mailboxfabric tx_rptr = likely REAL bug → E1-IO1 prerequisite backlog**, MIGRATION §5.2 #7-8). lint-mailbox narrowed to interface/-only advisory.
- **E0-SHL3 done:** `docs/performance-model.md` + `ethereal-tools/tools/perf_model.py` (self-checking). v1.1 tile = 548 bits (CLB320+SB120+CB108); 4×4 image = 1120 B (4 frames × 70 words incl. CRC16 tail); OCC hot-swap (blank+write+readback) 868 cyc = 17.36 µs @50 MHz (vs <10 ms Phase-1 target, ~500× margin); SPI@20 MHz = 448 µs (vs <100 ms). VPR virtual Fmax c432 = 195.66 MHz (v1.0 arch; v1.1 Wilton not re-characterized — open item).
- **E1-PLT4 done (open build chain PROVEN):** yosys synth_gowin → nextpnr-himbaechel `--device GW5AST-LV138PG484AC1/I0` (GW5AST-138C chipdb ships in oss-cad-suite) → gowin_pack → `spike_top.fs` (34.7 MB) on the REAL 4×4 fabric_top. Post-route **Fmax 115.61 MHz** (50 MHz target PASS; experimental timing data caveat). **LUT overhead ≈ 308:1** (39,474 cells / 128 virtual LUTs) — dominated by W=12 Wilton SB mux trees + per-tile cfg regs; **vs E1-PLT1 acceptance ≤45:1 → NOT met, feeds E2-FAB5 interconnect v2**. Gotchas: yosys SV subset rejects `return` in automatic functions (2 repo sites); nextpnr needs full part number + scalar probe wrapper ports; `--ignore-loops` for STA (Wilton cfg-dependent loops). Runtime: synth ~75 s, P&R ~15 min → CI-viable as nightly/gated job. openFPGALoader has tangmega138k support.
- **Ed25519 fw crypto done (E1-RUN2 foundation):** in-house RFC 8032 verify-only + freestanding SHA-512 (`ethereal-runtime/bmc-fw/crypto/`). 144/144 host vectors (RFC KATs + 64 PyCA cross + 64 tamper + 9 edge rejections: S≥L, small-order A, non-canonical encodings). rv32imc build clean, text ≈ 5.7 KiB, no heap. Verify latency est. ~10⁷–4·10⁷ cyc (~0.2–0.8 s @50 MHz — NEORV32 iterative mul; precompute table possible later). Contract: `eth_ed25519_verify(sig,pk,msg,len)` + `eth_ed25519_selftest()`; verify target = manifest_digest 64-byte lowercase-hex UTF-8 (matches ethimg).
- **EMRI v0.2 frozen (spec-first):** EFP command block §3.2 — host↔daemon mailbox: EFP_CMD doorbell (daemon clears to 0 on accept; no read-to-clear, shared AXI port), EFP_REGION (0xFF=auto), EFP_IMG_WORDS, EFP_STATUS (9-state lifecycle), EFP_ERR (8 codes), IMG_DIGEST[8]@0x18, IMG_SIG[16]@0x50. Daemon hex-encodes digest → verify over 64B hex (ethimg-compatible). v0 keyring = single compiled-in trusted pubkey (E2-SEC1 does real key mgmt).
- **ADR-018 execution line entered tasks.yaml:** E2-AXI1 (done — 4 formal proofs), E2-RV1 (eth_rv RV64IMC), E2-RV2 (RV64GC+Sv39 → Linux on GW5), E2-DMA1/2 (eth_dma_mc / eth_dma_2d), E2-DRAM1 (eth_dram_ctrl wrapper).
- **E1-RUN2 daemon DONE (2026-09-01, report-E1-RUN2-daemon-20260901).** EMRI v0.2 EFP RTL window in emri_regfile (pure-RW storage, lint-clean, tb_emri_regfile extended) + constants 3-way sync (emri_pkg.sv ↔ emri_constants.py ↔ emri.h, 33 new parity checks) + C daemon `bmc-fw/daemon/daemon.c` (~440 lines, no malloc, -Werror clean): VERIFY (hex64 digest Ed25519 vs keyring.h) → ALLOC (static region table, 0xFF=auto) → BLANK → LOAD (WRITE armed BEFORE LOAD visible — anti-deadlock ordering; host streams OCC_WDATA) → READBACK → RUNNING; stop/restart/abort; sticky EFP_ERR (bad_sig/region_full/occ_crc/occ_reject/bad_cmd/img_len). **tb_bmc_daemon capstone: 7 stages ALL PASS (41 ok checks, Verilator --timing, wall 1016 s), independently re-run by main agent** — sub-agent's "LOAD hang" was a stale build, not a code bug. Deterministic test keyring via gen_daemon_vectors.py (fixed-seed, PyCA-signed A/B images + tampered variant). E1-PLT1 hal/gowin_gw5 also DONE (deviation: 308:1 vs 45:1, maintainer sign-off pending).
- **🔴 NEORV32 netlist IMEM is PHYSICALLY 1 KiB** (`imem_rom` reads `addr_i[9:2]`, 256 words — NOT the labeled 16 KiB): the ~8 KiB daemon firmware cannot ROM-boot. Sim workaround = DMEM-exec bootstrap (1 KiB ROM stub + TB backdoor preload of 4 dmem_ram byte-lane sprams; vendored netlist untouched). **E1-BMC1 upgrade item for real HW: regenerate netlist (IMEM ≥16 KiB) or ROM→DMEM bootloader.**
- **Ed25519 verify ≈ 83M cycles** on this netlist (iterative mul, no FAST_MUL per ADR-017) ≈ 1.7 s @50 MHz real-silicon (fine vs 30 s ethctl target) but ~hours under iverilog → tb_bmc_fw + tb_bmc_daemon run under **Verilator --binary --timing** (cycle-count exemption from ADR-018 §7.5 iverilog rule, approved; other 26 TBs stay iverilog). **Maintainer question: enabling NEORV32 FAST_MUL_EN (behavioral DSP inference) would ~30× the verify — ADR-017 tradeoff to rule on.**
- **cocotb local gap CLEARED (2026-09-01):** smoke Makefile included stale `Makefile.simules` → `Makefile.sim` (correct for cocotb 1.9 Docker + 2.x .venv); test_counter.py had a never-run read-sync race (samples before NBA settle) → fixed with ReadOnly + Timer idiom + `unit=` rename. `make sim` 2/2 PASS in the project .venv (py3.12 + cocotb 2.0.1; PATH = .venv first, oss-cad-suite second).
- **Suite state:** make lint OK (incl. graduated mailbox core loop) / test-model 2639+3xfail / formal 4 PASS / `make sim` cocotb 2 PASS. Stale generated/ artifacts (v1.0-era frame_map_4x4.json/blank_4x4.hex) refreshed to v1.1 (548 bits, 70 words/frame).

## Progress log (2026-09-02 — RUN3 wave + hardening)
- **E1-RUN3 DONE (sim scope, report-E1-RUN3-ethctl-efp-20260901):** `efp_client.py` (transport-agnostic EFP session builder per spec §3.2: stage/cmd/stream/poll + `ethereal.efp-session.v0` JSON) + `ethctl --transport efp|mfsm --emit-session` (mfsm default intact) + new stop/restart/abort subcommands. **tb_ethctl_replay: ethctl-generated run/stop sessions replayed on the REAL daemon chain — RUNNING r0 + TFF toggle, STOPPED + blanked (wall 487 s).** 27 pytest (11 parity + 16 new). Follow-up recorded: daemon packed-deploy mode (per-column OCC_DECODE loop) needed for production bit-packed .eth.
- **🔴→✅ eth_axi_xbar cross-master response-aliasing BUG (found by RUN3 wave):** B/R/W owner scans OR-merged per-master state bits; two masters with same-destination in-flight reads aliased (host poll received BMC's IMG_DIGEST data → spurious LOAD → skid deadlock). This was the TRUE root cause of the earlier intermittent "LOAD hang" (misdiagnosed as stale build). Fixed with per-destination wr_busy/rd_busy recorded at issue, cleared at completion (implements the documented v0 lock-step). **All 6 xbar TBs + tb_bmc_daemon + tb_ethctl_replay re-run PASS post-fix (deterministic).**
- **NEORV32 IMEM 1 KiB RESOLVED:** root cause = `image_size_c` in `neorv32_imem_image.vhd` (default 796 B), NOT the IMEM_SIZE generic. Regenerated 16 KiB physical ROM netlist (same node names → TB backdoors intact) **swapped in as the vendored default** (provenance header refreshed); tb_bmc_hello byte-identical + >1 KiB exec probe (old netlist FAILs, new PASSes). Unblocks real-HW ROM boot; E1-BMC1 GW5 half still pending.
- **interface/ tree fully clean:** #8 uart MULTIDRIVEN was a REAL bug — `logic tx_finished = ...` static-init made both tx_rptr drivers dead → TX FIFO never drained, byte 0 re-sent forever (proven: OLD emits 11 11 11…, NEW emits 11 22 33 44 55 66). Fixed consume-on-load, single-owner. #7 spi_sat BLKSEQ hoisted (equiv-verified 50k cycles). lint-mailbox = 0 warnings whole tree.
- **E2-FAB5 spike DONE (report-E2-FAB5-interconnect-opt-20260901):** recommended **v2c = CB Fc=0.5 stratified × feedback-first IIB (fb full + ext parity-halved), SB Wilton unchanged** → 27,190 LUT = 212.4:1 = **−31.1%** (acceptance ≥20% met, 14% margin), c432 29/29 routes 5/5 seeds. −50% needs VPR re-pack freedom (a1) / W reduction / LUTRAM cfg — documented. Methodology note: synth_gowin abc post-map is chaotic on comb-loop netlists; dual post/pre-map columns in the report. Next: implement v2c in RTL + models + frame_map (spec-first).
- **Suite state (2026-09-02, all personally re-run):** make lint OK / **test-sv 31 TBs ALL PASS** (incl. tb_ethctl_replay; tb_bmc_daemon deterministic post-xbar-fix) / test-model 2655+3xfail / formal 4 PASS / lint-mailbox 0 warnings / cocotb 2 PASS.

## Progress log (2026-09-02 PM — v2c implementation wave)
- **E2-FAB5 v2c IMPLEMENTED + acceptance MET on real RTL.** Spec §7 frozen (interconnect-config-v0.md v0.2): CB Fc=0.5 stratified (18 inputs kept; per-input track fan-in 48→24; sel = 5-bit subset index k, `clb_in[i]=pool[(i%2)+2k]`, k=24..31 MUST-NOT-PROGRAM) × feedback-first IIB (pool ext[17:0]/pad/fb[31:24]; sel[4]=1→ext `2*sel[3:0]+π(m)`, sel=0→fb j0; all 8 fb visible per mux; ext parity-halved) × SB unchanged. Tile 548→530 bits; 4×4 image 1120→1088 B; frames 70→68 words. **Real-RTL measure: 39,474→29,869 LUT = 233:1 = −24.3%** (acceptance ≥20%; spike prototype predicted −31.1%, post-map abc noise documented in spike report). c432: 29/29 routes, 256/256 bit-true direct + 32/32 via full frames on v2c; **present_round flipped xfail→PASS** (don't-care drop kills fake routing demand; exposed+fixed a latent VPR buffer-absorption PO-alias bug in bitgen_db). fir16/aes remain xfail (fir16: 23 over-use @200it measured on v2c).
- **frame_decoder v2c gap (integration catch):** frame_decoder.sv hardcoded CB_SELW=6/548-bit tiles — parameterized with CB_DIV (default 2; CB_DIV=1 ≡ v1.1), MAX_WORDS 530-based; tb_frame_decoder NWORDS 35→34 + cb_sel_17 expectation 40→20 (k semantics).
- **tb_bmc_axi_fabric X-luck bug fixed:** v1.1 TFF pass relied on X-propagation luck; v2c's blank default (fb j0 self-read) makes an unreset FF stuck at X. Added dedicated fabric region-reset pulse after WRITE decode (fab_rst_n; shared rst_n would reboot the BMC). Real deployments do region-reset-after-config — pattern now codified.
- **eth_axi_xbar formal proof added:** per-destination in-flight uniqueness + owner-routing + consistency invariants proven (k-induction depth 40, 7 cover witnesses) — the E1-RUN3 aliasing bug class is now regression-proof. `make formal` = **5/5 PASS** (auto-discovered via formal/*.sby find, no Makefile edit needed).
- **EFP KAT updated** (test_ethctl.py) for the v2c daemon image A digest (sel 18→0 translation in gen_daemon_vectors.py); 27/27 pass.
- **Suite state (2026-09-02 PM, all personally re-run):** make lint OK (incl. frame_decoder) / **test-sv 31/31 PASS (exit 0, 33m38s)** / test-model 2668+2xfail / formal 5/5 / perf_model self-check OK (68-word frames, 1088 B).
- **Open carry-over:** daemon packed-deploy mode DONE (2026-09-02 late): EMRI v0.3 (EFP_IMG_COLS@0x21 — 0x19 collided with IMG_DIGEST, caught by sub-agent; EFP_CMD 5=run_packed; §3.3 per-column BLANK→OCC_DECODE→WRITE→READBACK loop with done_flag host handshake); tb_bmc_daemon_packed 26 checks PASS (packed TFF deploy on real fabric + tamper reject + stop); tb_bmc_daemon legacy path unchanged-green. EFP-SPI transport (E1-IO1) in flight: SDI-enabled netlist candidate regenerated + verified (16 KiB IMEM retained, hello byte-identical, n6964→n7280 node renumber noted); SDI CSR map 0xFFF70000 documented (report-E1-IO1-sdi-regen-20260902). A1 assumption (cluster-pin duplication freedom) flagged for maintainer in v2c-tools report; fir16 homogeneous routing ceiling remains (DSP-T path is the answer).
- **E1-IO1 EFP-SPI DONE (2026-09-02, sim scope):** SDI netlist swapped in (vendored default now: 16 KiB IMEM + SDI; all 10 BMC TB backdoors n6964→n7280); bmc_core exposes sdi_clk/csn/dat_i/dat_o; `bmc-fw/efp-spi/` (sdi.c byte driver + efp_spi.c 7-byte §7 frame engine, no malloc, -Werror clean, fw 10,008 B); spec §7.1 CRC16 addendum (CRC over OCC_PUSH stream, WR 0x3F, STATUS=0x04 CRC_ERR, EFP_ERR 8=crc_transport; daemon gate at LOAD→READBACK — deviation documented, outcome identical: never RUNNING + re-blank). efp_client.to_spi_frames + 5 new pytest (2677 total). **tb_bmc_spi capstone PASS ×2: SPI bit-bang host drives full packed TFF deploy → RUNNING → fabric toggle → stop → blank; CRC bit-flip injection → CRC_ERR + fabric untouched.** Key HW finding: NEORV32 SDI reloads TX shift-reg 3 clocks after each pop (FIFO depth 1 unserviceable) → v0 link pulses CS per byte + firmware TX re-arm + 0xFF-retry resync — documented, v0.1 = IRQ/deeper-FIFO candidate. Rare (~1/1000) spurious host resends absorbed by design (idempotent WRs + BUSY guard).
- **Suite state (2026-09-02 final, all personally re-run):** make lint OK / **test-sv 33/33 PASS (exit 0, 57 min)** / test-model 2677+2xfail / formal 5/5 / lint-mailbox 0 / cocotb 2/2.

## Progress log (2026-09-11 — DMO1 closure + EMRI v0.4/v0.5 + fwupdate)

- **E1-DMO1 CLOSED (sim scope, commit `38cc151`; report-E1-DMO1-demo-images-20260902 updated).** Two signed demo images ship (pwm 11 eLUT / uart_loopback 77 eLUT, Ed25519 vs the daemon keyring) + `run_packed` EFP session JSONs. Root cause of the long-open uart tick failure was **not** the tick model: `bitgen_db._tile_output_nets()` (the `driven` set feeding buffer-alias `canon`) still read the LUT-leaf (pre-FF D-side) net while `_build_tile` pass 1 had moved to the FF Q side → the `rxs[1]`→`rx_stop` buffer alias never unified → **5 cluster inputs silently unrouted** (default tracks) → wrong sequential logic (first divergence tick 914). Fix: `_fle_out_net()` single source of truth + demand-position lookup; test-side decoder/stream-tail fixes. uart now tick-bit-true for all 6873 cycles + semantic decode `55 00 FF A7 3C C3` (framing-error 0x5A dropped); unrouted 0 (was 5).
- **E1-BMC2 CLOSED (sim scope, report-E1-BMC2-fwupdate-20260911).** Dual-partition FW self-update demo (`bmc-fw/fwupdate/`: XIP slots in a TB flash model, boot stub CRC + last-known-good fallback, `EFP_CMD` 6/7 sim-demo) — tb_bmc_fwupdate **30/30**; the only defect was the boot stub's missing `boot: slot <X> v<ver>` print line (three TB waits had been timing out at 2 s each). All three E1-BMC2 acceptance items now have evidence (UART boot / region lifecycle / FW self-update).
- **E1-RUN4 CLOSED (sim scope, report-E1-RUN4-watchdog-20260911).** OCC op watchdog (2^20 budget → drain-complete + BLANK + `EFP_ERR=9` + event 1) + region heartbeat + event-log ring (§3.4); tb_bmc_watchdog **29/29**. Two real defects found by the new capstone: ① `frame_decoder.frame_base_i` hardwired 0 in **every** system TB → any deploy at a nonzero frame base (region>0 / col>0) mis-decodes (the watchdog TB is the first to deploy to region 1); ② `occ_top`'s single `write_crc_r` "last write wins" compare → the heartbeat **false-quarantines healthy regions** and `cols>1` deploys can never pass their own READBACK (never surfaced: tb_bmc_daemon_packed stages `IMG_COLS=1`). Fixed per **ADR-019 / spec v0.5 §3.1.1**: READBACK now compares against software-supplied **`OCC_EXPECT_CRC`** (`0x0E`, latched at accept) + **`OCC_CRC_RESULT`** (`0x0F`, exposes the running CRC) — the daemon latches each column's CRC after its WRITE and supplies it before every deploy/heartbeat READBACK. Also fixed `OCC_WDT_POLL_BUDGET` `1000000u` → `(1u<<20)` (spec drift).
- **Suite state (2026-09-11, all personally re-run):** make lint OK / model **2683 passed + 2 xfail** / ruff clean / firmware `-Werror` clean. Sim capstones all PASS: tb_bmc_watchdog 29, tb_bmc_daemon 39, tb_bmc_daemon_packed 25, tb_bmc_spi 31, tb_ethctl_replay 18, tb_bmc_fwupdate 30; 11 unit/iverilog TBs PASS (tb_occ, tb_blank, tb_emri_regfile, tb_emri_occ_loop, tb_bmc_axi_{emri,occ,fabric}, tb_mgmt_hotswap, shell_tb_{mgmt,het}_packed, tb_frame_decoder).
- **Open carry-over:** other system TBs still hardwire `frame_base_i=0` (base-0-only today; fix alongside the DMO2 4-column replay); the mFSM host flow (E2-BMC1) must arm `OCC_EXPECT_CRC` before READBACK; **E1-DMO2 can now replay the 4-column uart session** (v0.5 removed the architectural blocker).

## Progress log (2026-09-11 PM — E1-DMO2 hot-swap stability)

- **E1-DMO2 DONE (sim scope, report-E1-DMO2-hotswap-stress-20260911).** 两层证据：**OCC 层字面 2×10000** —— 新 TB `tb_occ_soak`（20000 次 BLANK→WRITE→READBACK，每 region 10000、镜像 A/B 交替）逐交换断言 READBACK（v0.5 expected-CRC 门）、`crc_error` 恒 0、RAM 影子（目标==镜像、**邻区逐字不变**）→ **0 errors / 0 word mismatches**，1.5 s wall（26.7 ms sim/s）。**daemon 层轮换** —— 新 TB `tb_hotswap_stress`（6 轮，2 region × 签名镜像 A/B）：每轮 STOP（逐列 BLANK）→ run_packed → RUNNING + 三层校验（EFP_ERR=0 / RAM 影子 / 真实织物输出签名 + 邻区不受扰，需先 `fab_region_reset`）→ **69 ok / 0 fail**。字面 daemon 2×10000 不可行（每轮一次 Ed25519 ≈0.84 s sim → 20000 轮 ≈22–32 天 wall）→ 分层归硅范围（E1-PLT2）。
- **新发现的运维/设计契约（写入报告）**：① `alloc_region` 只接受 FREE region → **跨镜像热替换 = stop+deploy**（`restart` 仅是"同镜像重跑"）；② TB 的 UART 包含匹配必须**消费式**（旧的"从队列头扫描"会让重复字符串匹配到上一轮的副本，脚本抢跑并与 daemon 竞争 doorbell → `region_full`/丢 doorbell）；③ **多列地址别名（结构性，待修 E1-DMO2b）**：`OCC_FRAME_ADDR` 的列字段 [11:4] 只给 16 词/列，而 v2c 列帧 34–68 词 → 同 region 内相邻列在回读存储重叠 → 多列部署 READBACK 必然失配（单列/每 region 一列不受影响）。方案：spec v0.6 列基址 `col[11:8]`、词 `[7:0]`（256 词/列）+ daemon `c<<4`→`c<<8`；DMO1 遗留的 4 列 uart 回放依赖此修复。
- **工具**：`gen_daemon_vectors.py --frame-hex-b`（第二签名镜像，digest+Ed25519）+ `make stress` 独立目标（OCC soak + daemon 轮换；长时 TB 不并入 `test-sv`）。

## Progress log (2026-09-11 evening — E2-FAB3 context save/restore)

- **E2-FAB3 DONE (sim scope, report-E2-FAB3-ctx-scan-20260911).** Spec `ethereal-spec/fabric/ctx-scan-v0.md`（链拓扑/打包/驱动顺序/EMRI 拆期）+ RTL：`elut4`/`clb_t`/`fabric_top` 增 `scan_en_i`/`scan_in_i`/`scan_out_o`（FF 优先级 rst > scan > CE；链序号 `e = tile_rm*8 + gi`，覆盖**全部** eLUT vff，与配置无关）+ 新引擎 `ethereal-fabric/rtl/occ/ctx_scan.sv`（SAVE 采样打包 `element e → word e/32 的 bit e%32`；RESTORE 升序读字、MSB-first 驱动；单时钟域）。**capstone TB `tb_ctx_scan` PASS**：双 fabric 锁步 12 周期 → A 暂停+保存（窗口字 bit0 == 暂停点状态）→ 恢复 → **与不间断参考逐周期比对 24 周期位精确**（镜像为自触发 TFF，相位/位错即失败）。`make lint` OK；8 个受影响 TB 重跑 PASS（含 3 个 emri iverilog TB）；15 个测试文件扫描端口 tie-off 逐文件校验。
- **两个镜像缺陷（TB 抓出并修复）**：① SAVE 累加器方向反（先采到的 element N-1 落在 bit0）→ 改 `acc <= {acc[30:0], scan_out}`；② RESTORE 按 LSB-first 驱动、与 ① 组合成**双重反向**（修 ① 后暴露）→ 改 MSB-first（`sreg <= {sreg[30:0], 1'b0}` + `scan_in = sreg[31]`），并同步修正 spec §4（链每移位一次元素上移一位 ⇒ 驱动顺序必须与采样顺序对称）。教训：链式读写的方向必须由"采样顺序 ↔ 驱动顺序"的对称性推导，不能各写一套。
- **诚实边界**：TB 目前只覆盖 **words=1（32 位链）**，多字链路径未覆盖（→ E2-FAB3b 扩展）；真实时钟切换/SSRAM 窗口映射仍为 hal/glue ASSUMPTION；未配置瓦片的 vff 保持 X 语义（保存字高位 X 属预期）。

## Progress log (2026-09-11 night — E2-SEC1 安全 v1 + E1-DMO2b 列寻址 v0.6)

- **E1-DMO2b DONE（EMRI v0.6）**：`OCC_FRAME_ADDR` 重映射为 `{region[15:12], col[11:8], word[7:0]}`（256 词/列窗口 ⇒ 相邻列不再别名；v0.5 的 16 词步长与 34–68 词 v2c 列帧重叠）；`daemon.c` 新增 `column_frame_base()` 单一实现并替换 4 处 per-column 调用；工具侧 `region/column_frame_base + column_frame_range(>256 词拒绝)`、packed 模型逐列 arming、`frame_map.frame_addr`/`frame_addr_format` 同步迁移。证据：fw `-Werror` 干净；工具 pytest 406 passed（含 2 列地址不相交与窗口不重叠断言）；单列路径零回归。遗留：≥2 列真实 daemon 路径回放 → `E1-DMO2c`；mFSM `frame_addr` 语义 → E2-DOC1。
- **E2-SEC1（安全 v1）**：规范 `ethereal-spec/security/capabilities-v0.md`（**带类型条目**：`io:[{group,dir}]`、`services:[{name,access}]`；三段校验 schema→可授予性→分配绑定）+ EMRI **v0.6** §3.7/§3.8（能力门 0x22-0x24、异常监控 0x32-0x37、`EFP_ERR=11/12`、事件码 4/5）。实现三面：① 主机（`ethereal-runtime/security/capcheck.py` + ethimg/efp_client/ethctl）——EFP 路径**默认强制 Ed25519**（此前完全无主机侧验签）、CAP_DECL 前置写入、越权声明拒绝并打印具体条目；② RTL（emri_regfile）——硬件能力比较器 + `CAP_STATUS`、4096 时钟/tick 窗口监控、逐区饱和计数、spike→flag+throttle+`code=5` 事件（环 push 与总线 push 在 regfile 内仲裁，**不新增端口**；新 TB 抓出并修复 2 个真实缺陷）；③ daemon——ALLOC 后/BLANK 前读 `CAP_STATUS`，denied→11 且不 BLANK（AUTO 回退 FREE），throttle→12，RUNNING/看门狗事件写 `MON_NOTIFY`。证据：`make lint` OK、`make test-model` **2724 passed + 2 xfailed**、7 个 iverilog TB（含新 `tb_mon_anomaly`）PASS、fw `-Werror` 干净、daemon Verilator TB 通过。
- **明确的 v1 边界（写入报告）**：sim 允许掩码（IO 0–7/服务 0–7）代替 Board Manifest（E1-IO3/E2-IO1 替换）；`denied_io` 在 sim 掩码下恒 0（语义按规范=越权集合低字节）；监控范围 = `NUM_REGIONS`（v0=2）；窗口/阈值为带 ASSUMPTION 的默认值（0x1000 tick ≈160 ms、recfg 16、wdt 8）；region 锁矩阵仍是单门+硬连线 0（S02/E0-FAB5 线，非本任务）。
- **方法学教训（再次验证）**：能力清单三处（主机/RTL/daemon）必须由**冻结规范**单点驱动 —— 本轮 spec 先行后三个 agent 并行实现零返工；唯一冲突（`CAP_DECL` 是否随 doorbell 清除）由实现方按规范文本顶回，避免了"门永远看到空声明"的假安全。
- **回归教训（列寻址 v0.6）**：ABI 字段语义变更后**测试里的地址断言也是调用方** —— `tb_hotswap_stress` 与 `tb_occ_soak` 各自硬编码 `reg_base(r)=(r<<12)|(r<<4)`，daemon 改 `<<8` 后 region 1 的镜像落到 0x1100 而断言检查 0x1010 → `make stress` 立即 FAIL（5 errors / 10 词失配），迁移断言后 69 ok / 0 失配。判据：改寄存器字段划分时先 grep `<<4`/`<<12` 类地址算术，把 TB 与工具一并纳入迁移清单。

## Phase 0 progress log (2026-07-24)
- **Wk1 infrastructure ✅ (committed `78ab251`):** E0-INF1 (8-repo skeleton) + E0-INF2 (CI) + E0-INF3 (Docker+Makefile+smoke) + E0-INF4 (trademark) + S04-P0#1 (mailbox migrated to ethereal-shell, CERN-OHL-S, lint cleanup pending `S04-P0#2`). Docker-gated validations pending.
- **E0-FAB1 ✅ (elut4+FF):** `ethereal-fabric/rtl/clb/elut4.sv` G1-clean (in lint glob); golden model `elut4_model.py` validated by **1211 local pytest**; cocotb DUT-vs-model Docker-gated. Bitfield frozen in `ethereal-spec/fabric/elut4-config-v0.md`. Report: `docs/reports/report-E0-FAB1-elut4-20260724.md`.
- **Convention established:** `make test-model` = local pure-Python golden-model pytest (no simulator); cocotb tests `test_<unit>.py` (Docker-gated), model tests `test_<unit>_model.py` (local).
- **OSS-CAD local validation ✅ (2026-07-24):** OSS-CAD Suite (`~/oss-cad-suite`: Verilator 5.051/Yosys 0.67/iverilog 14) located. `make lint` now **clean** (lint caught+fixed 2 real syntax bugs `(x)[range]`→`WIDTH'(x)`, WIDTHEXPAND, 2× UNUSEDSIGNAL; documented `-Wno-UNOPTFLAT` for intended CLB/routing loops). 3 self-checking **SystemVerilog testbenches** (tb_elut4/clb_t/switch_box) **PASS** via new `make test-sv` (iverilog). Golden-model **1876 pytest pass**. cocotb has py3.11/3.12 + v1/v2-makefile mismatch → SV TBs are the local DUT validation. Docker now OPTIONAL (only VPR/CI). New Makefile: `test-sv`, IVERILOG detect, lint scoped to `rtl/` + per-call `-Mdir`. Report `report-validation-oss-cad-20260724.md`.
- **S02-P0#1 ✅ (frame-map gen):** `ethereal-tools/tools/frame_map.py` — pack/unpack/blank/json + CRC-16/CCITT tail word; frame = one column's config bits, addr={region[3:0],col[7:0]}, blank=safe-zero. **309 pytest pass** (300 round-trip + CRC-tamper + blank + geometry); `make test-model` now **2185 total** (extends find to ethereal-tools). Artifacts (frame_map.json/blank.hex) → gitignored `generated/`. Report `report-S02-P0#1-framemap-20260724.md`.
- **E0-FAB4 ✅ (OCC v0):** `ethereal-fabric/rtl/occ/occ_top.sv` — cmd FSM (IDLE→WRITE/BLANK/READBACK→CMP), write_engine, frame bus, streaming CRC32, per-region LOCK. **lint-clean strict -Wall (zero warnings)**; `tb_occ` (iverilog) **PASS**: WRITE→READBACK CRC ok, tamper detected, BLANK zeroes frame, LOCK blocks WRITE. `make lint` clean-loop refactored to cover occ_top (was hardcoded per-module — fixed); `make test-sv` now 4 TBs. v0 = **config-path verified**; running a real circuit needs the routable CB (deferred). Report `report-E0-FAB4-occ-20260724.md`.
- **E0-FAB5 ✅ (blank-before-write + LOCK):** occ_top extended — per-region `dirty` bitmap + `S_NEEDS_BLANK` status: WRITE to a dirty region is rejected (C03 §3 red-line, now HARDWARE-enforced, not just BMC-protocol); BLANK clears dirty, WRITE sets it, reset=clean; LOCK has priority over dirty. **lint-clean**; `tb_blank` **PASS** (6 checks incl. **region isolation** — writing region 0 doesn't touch region 1's config storage; R0 dirty doesn't block clean R1); tb_occ regression PASS after adapting check 3 to a fresh region (DEPTH bumped to 8192 for region-1 addrs). `make test-sv` now **5 TBs**. v0 = config-storage-level isolation; fabric-output glitch prevention deferred (needs region gates + routable CB). Report `report-E0-FAB5-blank-20260724.md`.
- **E0-FAB6 ✅ (fabric-gen v0):** `ethereal-tools/tools/fabric_gen.py` — fabric.yaml/json descriptor → frame_map.json + manifest.json + blank.hex (calls FrameMap; `fabric_top` is parameterized so it emits params + frame_map, not bespoke RTL). 2×2/4×4/2×3/3×5 frame_map round-trip valid (→ OCC/bitgen-usable); geometry verified (4×4 = 16 tiles, 6656 bits, 4 frames, 212 words). Reference descriptors `ethereal-spec/fabric/fabric_{2x2,4x4}.yaml`. `make test-model` now **2197**. **Fabric core (E0-FAB1..6) COMPLETE.** Report `report-E0-FAB6-fabric-gen-20260725.md`.
- **E0-MAP1 ✅ (synth_ethereal):** `ethereal-tools/tools/mapper/yosys/synth_ethereal.py` — Yosys front-end (`synth -auto-top; abc -lut 4` → eLUT4 netlist; `$lut WIDTH=4` ≡ eLUT4 truth table). **c17 → 2 eLUT4, c432 → 62 eLUT4** (real ISCAS85, both in the reasonable band; c432 0 FF combinational). 4 pytest PASS (skip if no yosys); `make test-model` now **2201**. Flow documented in `synth_ethereal.ys`. Benchmarks c17.v (MIT) + c432.v (ISCAS85 public-domain, provenance header). Report `report-E0-MAP1-synth-20260725.md`.
- **Routable CB Step 1 ✅ (switch_box injection + input CB ready):** `switch_box.sv` v2 — `clb_out_i[N_INJ]` + inject_en (out_e[j<N_INJ] = inj? clb_out[j] : disjoint, **override not merge**, SB stays single driver); `connection_block.sv` (new, lint-clean) — clb_in = mux of 4*W tracks (input CB, **ready, not yet instantiated**); `fabric_top.sv` wired clb_out_i (tap still in place for clb_in — Step 2 will replace). Sub-agent updated sb_model/fabric_model/tb_switch_box (668 model tests; tb_switch_box PASS; default acyclic). **make lint OK / make test-sv 5 PASS / make test-model 2211** (personal re-verify). Caught: `inj_en_r` reset-less → X propagation → TB zero-inits all 56 cfg regs (same lesson as clb_t). cfg_addr 6-bit (48 sel + 8 inject). Report `report-routable-cb-step1-20260725.md`.
- **Routable CB Step 2 ✅ (input CB integrated + end-to-end routability PROVEN):** `connection_block` now instantiated in `fabric_top` (one/tile, replaces minimal tap) — cfg `unit` field widened to 2 bits (00=CLB/01=SB/10=CB), `tile_idx` shifted to [7+TIW:8]. New `cb_model.py` (ConnectionBlock golden model, pool {n=0,s=W,e=2W,w=3W}, sel default 0=post-zero-init HW-accurate). `fabric_model` extended: `_cb_edges` + `route_exists` BFS. NEW `tb_connection_block.sv` (zero-inits 18 sel_r, 4-dir bit-true + negative checks) → `make test-sv` now **6 TBs**. **`make lint OK / make test-sv 6 PASS / make test-model 2483`** (personal re-verify). **Routability PROVEN**: `route_exists(tile0.clb_out[0]→tile1.clb_in[0])`=True via inject→channel→SB→CB; negative control False. Default-acyclic holds (CB edges end at clb_in sinks). Report `report-routable-cb-step2-20260725.md`.
- **Routable CB COMPLETE (Step 1+2).** The fabric is now **end-to-end routable** — unlocks E0-MAP2 (VPR arch) + the Phase-0 circuit-run / hot-swap demo.
- **E0-MAP3 increment 1 ✅ (bitgen LEVEL-1 config DB):** `ethereal-tools/tools/mapper/bitgen/{bitgen_db,bitgen_sim,test_bitgen}.py` — parses VPR `.net`/`.place`/`.blif` → fabric-independent per-tile config semantic DB (eLUT4 TT + FF + IIB mux sel + placement). **c17 bit-true PROVEN: 32/32 input combos vs independent iverilog golden on the original `c17.v`** (crossbar-derived pin permutation; port_rotation_map[i]=logical pos carried by physical pin i; identity=[3,2,1,0] MSB-first). c432 structural: 9 tiles / 62 eLUT4. 8 bitgen tests + 2491 total model pass; lint OK / 6 SV TB / ruff clean. **FIXED RECORD:** `cfg_data[19:4]=tt, [3]=ff_en, [2]=ff_rst_en, [1]=ff_rst_val, [0]=out_inv` (RTL+spec ground truth — my earlier brace-notation summary was ambiguous; bitgen follows RTL, enforced by test). Report `report-E0-MAP3-bitgen-level1-20260725.md`.
- **E0-MAP3 increment 2 ✅ (frame_map SoT gap FIXED):** extended `frame_map.py` to match the routable-CB fabric — added SB `inject_en` (N_INJ=8 × 1 bit) + `connection_block` (N_CB=EXT_IN=18 sel × $clog2(4W)=6 bit) tile types. New tile = CLB(320) + SB(96+8=104) + CB(108) = **532 bits** (was 416). Updated `interconnect-config-v0.md` spec (inject_en cfg, connection_block section, routable-CB §4, open-items). Updated test_frame_map + test_fabric_gen geometry (4×4: data_words 52→67, words 53→68; 2×2: 26→34/27→35; tile_points 88→114). fabric_gen `tile_bits` comment. Suite green: lint OK / 6 SV TB / **2491 model**; regen 4×4 artifacts confirm (tile_width 532, 114 points). SoT now consistent for LEVEL-2 packing.
- **E0-MAP3 increment 3 ✅ (bitgen LEVEL-2 frame packing):** `ethereal-tools/tools/mapper/bitgen/bitgen_pack.py` — DB → frame_map frames: `db_grid_bounds` (normalize VPR (x,y) → fabric (col,row) bounding box), `tile_to_config_points`/`config_points_to_tile` (CLB points only; SB/CB blank = incr 4), `db_to_frames`/`frames_to_db`. Added `elut_from_word` (inverse of elut_cfg_word) to bitgen_db. **c17 functional-after-pack: 32/32** (pack→unpack→reconstruct→re-attach netlist→simulate vs iverilog golden) — proves frame packing is lossless + logic-correct. c432 round-trip: 9 tiles/62 eLUT4 preserved, 4 frames × 51 words (R=3,C=4). 13 bitgen tests, 338 total mapper, lint OK, ruff clean. NOTE: frames carry config only (no net names) — netlist context re-attached at apply time (OCC/sim harness); c432 sparse placement → gaps reconstruct as blank tiles (frame = whole physical column). Report `report-E0-MAP3-incr3-bitgen-pack-20260725.md`.
- **E0-MAP3 increment 4a ✅ + 🔴 Phase-0 FINDING (custom router; v1 unroutable for c432):** `ethereal-tools/tools/mapper/bitgen/bitgen_route.py` — PathFinder on the REAL fabric topology (Option B of ADR-012 refine; VPR pack/place only, we route on the actual switch_box/connection_block graph → guaranteed realizability). `extract_nets`/`route`/`apply_route_to_grid`. Router VERIFIED CORRECT (synthetic 1×2 routes; converges on c432's feasible subset, route_exists-confirmed; 4 route tests pass + 1 xfail). **🔴 FINDING (independently verified): c432 is UNROUTABLE on the v1 fabric** (29 inter-cluster nets; 0/29 converge, persistent over-use). Two structural causes: **(1) east-only inject** strands drivers at the eastmost column (inject emits only out_e[j]; no east neighbor → signal trapped — verified: west-edge driver routable=True, east-edge stranded=False); **(2) disjoint-SB track-locking** over-subscribes tracks (disjoint SB preserves track index → each net locked to t=driver_j; track 7 has 7 nets, track 2 has 6 → can't coexist). VPR packs to fle-index with no awareness of these v1 constraints. This is EXACTLY what Option B was built to surface (VPR's abstract rr_graph would have hidden it). Report `report-E0-MAP3-incr4a-route-20260725.md`. **G6: maintainer decision required on fix direction** — options A (Wilton/track-flexible SB, fixes Cause 2, bigger) / B (bidirectional inject, fixes Cause 1, surgical) / C (track-aware packing, no fabric change, complex) / D (defer c432 to Phase 1 fabric v2, smaller Phase-0 demo). Sub-agent rec: B+C. 342 pass + 1 xfail, lint OK.
- **E0-MAP3 increment 4b ✅ (Option B-inject: bidirectional inject):** `switch_box` RTL changed east-only `inj_en[j]` → `inj_en[j]+inj_dir[j]` (2-bit, 0=N/1=S/2=E/3=W; each clb_out[j] injects onto ONE configurable direction). Widened SB cfg_data to 3-bit; fabric_top passes [2:0]. Ripple: sb_model (match RTL bit-for-bit, verified: inj-west→out_w[3]=1/out_e[3]=0), fabric_model, frame_map (SB +inj_dir_{j} 2-bit; SB 104→120, tile 532→**548**, 4×4 words/frame 68→70), tb_switch_box (inject E+W+N), all tests. **CAUSE 1 (east-edge stranding) FIXED** (0 stranded nets). **CAUSE 2 (disjoint-SB track-locking) REMAINS** → c432 still xfailed (track 7: 7 nets, track 2: 6 nets; structural, not congestion). Sub-agent caught+fixed a router bug (multi-sink net inject dir). 2517 pass + 1 xfail, lint OK, 6 SV TB, ruff clean. Report `report-E0-MAP3-incr4b-bidir-inject-20260726.md`. ADR `docs/adr/ADR-012-refine-v1-routing.md`.
- **🔴 OPEN DECISION (G6, ADR-012 refine):** v1 (disjoint SB) can't route c432/AES/FIR (Cause 2). Fix = **Wilton SB** (Option A-fabric) — CONTAINED (frame_map SB points unchanged; only switch_box source permutation + sb_model + arch `type=wilton`; Wilton lets nets change track index → breaks locking) and the spec's named v2 target. vs **Option D** (defer to Phase-1 fabric v2; validate Phase-0 flow on small routable circuits). Maintainer to choose. incr 4b-sim (IO injection + fabric sim) is independent + can proceed on any routable circuit.
- **E0-MAP3 increment 4c ✅ (Option A: WILTON SB — c432 ROUTES!):** `switch_box` source map disjoint(same-index) → **Wilton track-permuting Fs=3** (perm table mapped from S.Wilton thesis/VPR); `sb_model._wilton_track` matches RTL bit-for-bit (verified). arch `switch_block type=wilton`. **frame_map SB config points UNCHANGED** (still 48×2-bit sel) — contained. bitgen_route SB possibility-edges use the Wilton perm. **c432 CONVERGES: 29/29 nets, 46 iters, 0 over-use, ALL 57 driver→sink pairs route_exists=True** (independently re-verified). Cause 2 RESOLVED. Both Cause 1 (bidir inject) + Cause 2 (Wilton) fixed → **v1.1 fabric routes c432**. tb_switch_box Wilton source-track checks pass; 2579 pass / 0 xfail, lint OK, 6 SV TB, ruff clean. Hardened: route() default max_iters 30→100 (c432 needs 46). ADR-012-refine updated (open decision → RESOLVED, Wilton). Report `report-E0-MAP3-incr4c-wilton-20260726.md`.
- **E0-MAP3 increment 4d ✅ ★ ACCEPTANCE MET (fabric sim + c432 bit-true):** `ethereal-tools/tools/mapper/bitgen/fabric_sim.py` — bit-level `clb_eval_bits` (mirrors clb_t/elut4) + `FabricSim`/`simulate_fabric` (per-tile CB→CLB→Wilton-SB→channels, Gauss-Seidel combinational fixpoint, PI injection / PO observation). **c432 BIT-TRUE: 256/256 random vectors, all 7 POs, vs independent iverilog golden on c432.v** (avg 9.8 fixpoint iters, every vector converges). THE CAPSTONE — full chain synth→VPR→bitgen-DB→Wilton-router→**fabric-sim** validated end-to-end bit-true. **BUG FOUND+FIXED:** the acceptance gate caught a real `bitgen_db` incr-1 bug — sub-4-input LUTs (common after abc) stored the un-expanded logical TT into the 4-input eLUT slot → VPR's tied don't-care pins perturbed output (c17's full-4 LUTs never exposed it). FIX: `_expand_logical_tt` replicates the function over don't-care pin dims. Verified raw DB now bit-true; workaround + regression guard removed. 2582 pass, lint OK, 6 SV TB, ruff clean. Report `report-E0-MAP3-incr4d-fabric-sim-20260726.md`.
- **🎉 E0-MAP3 COMPLETE (incr 1-4d).** c432 bit-true in sim fabric = the E0-MAP3 acceptance. **v1.1 fabric** (Wilton SB + bidirectional inject) routes + runs c432. The mapping chain is validated end-to-end.
- **NEXT:** (a) extend `bitgen_pack` to pack routing (SB/CB/inject) into full frames (currently CLB-only; the sim used DB+RouteConfig directly — full-frame packing completes the bitgen deliverable for the OCC load path). (b) **E0-SHL** (Shell/EBI-Tiny + OCC frame-load) + **dual-image hot-swap demo** (Phase-0 exit) — loads full frames via OCC into the fabric RTL, runs 2 images, hot-swaps. (c) E0-MAP5 (benchmarks AES/PRESENT/FIR16/CRC32/PWM) — now routable on v1.1.
- **E0-MAP3 increment 4e ✅ (FULL frame packing — E0-MAP3 FULLY CLOSED):** extended `bitgen_pack`: `tile_to_full_points` / `full_to_frames` / `frames_to_route` pack CLB (db) + SB `mux_{dir}_{t}` sel + `inj_en_{j}`/`inj_dir_{j}` + CB `cb_sel_{i}` (rc) into COMPLETE frames (TYPE_CHECKING import avoids the bitgen_pack↔bitgen_route cycle). **Validated: c432 full-frame round-trip lossless + c432 BIT-TRUE VIA FULL FRAMES** (full_to_frames → frames_to_db + frames_to_route → FabricSim → bit-true vs iverilog golden) — closes the frame-packing loop end-to-end. cb_sel[i]=0 (reads out_n[0]) ≡ ConnectionBlock default ≡ absent (semantically lossless). Also fixed 2 pre-existing ruff F401 (synth_ethereal `sys`, test `_yosys`). 23 bitgen tests, 2584 total, lint OK, ruff clean.
- **🎉🎉 E0-MAP3 FULLY COMPLETE (incr 1-4e).** The bitgen emits OCC-loadable FULL frames; c432 is bit-true via the full frame path. **v1.1 fabric (Wilton + bidir inject) routes + runs c432 end-to-end.** The entire mapping chain (synth→VPR→bitgen-DB→Wilton-router→FULL-frames→fabric-sim) is validated bit-true.
- **NEXT PHASE: E0-SHL** (Shell/EBI-Tiny + OCC frame-load into the fabric RTL) → **dual-image hot-swap demo** (Phase-0 exit). Then E0-MAP5 (benchmarks).
- **E0-SHL hot-swap demo ✅ (Phase-0 EXIT criterion MET):** `ethereal-fabric/tests/interconnect/tb_hotswap.sv` — dual-image hot-swap on the **real fabric_top RTL** (iverilog). Image A = self-contained TFF (clb_out[0] toggles 1,0,1,0 via eLUT4 FF + IIB feedback); BLANK-before-write (C03 §3 red-line); image B = constant-1. **PASS: A toggles → blank → B constant, hot-swap works** (no glitch). Host direct-drive via fabric_top's cfg interface (the P0 circuit-breaker path; full EBI/OCC/frame-bus Shell = E0-SHL1/2 follow-up). X-propagation guard: eLUT4[0] made self-contained (all 4 IIB muxes read clb_out[0] feedback) so vin depends only on the clean FF, not X reset-less clb_in/CB/SB. Added to `make test-sv` (now **7 TBs**). 2584 model tests, lint OK.
- **🎯 Phase-0 EXIT status:** dual-image hot-swap ✅; bit-true (c432 via full frames, incr 4d) ✅; ADR-012 archived+refined ✅; suite green ✅. **Remaining P0 items:** E0-MAP5 (AES/FIR benchmarks — routable on v1.1 Wilton); full Shell E0-SHL1 (EBI-Tiny) + E0-SHL2 (EBI+OCC+frame-bus→fabric bridge) = the proper host path (demo used direct-drive); CI (Docker ethereal-sim image parity).
- **E0-MAP5 ✅ (benchmark set + golden infra + v1.1 routability CHARACTERIZED):** `ethereal-images/benchmarks/{pwm,crc32,fir16,present_round,aes128_round}.v` (new, all combinational-friendly) + `bitgen/bench_golden.py` (golden_comb: iverilog golden over N random vectors, multi-bit port handling) + `bitgen/test_bench_flow.py` (per-circuit full-flow bit-true, mirror of c432 acceptance). **pwm(11 eLUT)+crc32(42) bit-true PASS** (full chain). **fir16(124)/present_round(128)/aes128_round(4779) XFAIL** — measured: (a) fir16 adder-tree = 7-node over-subscription, no converge @300 iters (congestion); (b) present_round = **0 inter-cluster nets** (the pLayer permutation is entirely PI→cluster) + scattered tiles → input-CB track locality can't inject all 128 PIs (hard non-convergence); (c) aes128_round = **4779 eLUT4** (256-entry S-box case → `abc -lut 4` blows up the S-box; needs ROM-mapped S-box/LUT sharing/abc options, or accept as a Phase-1 H.264-scale test). **v1.1 Wilton routability ceiling = small/mid combinational circuits (c432/pwm/crc32); FIR/PRESENT/AES = Phase-1 fabric** (IO-routing model + larger/long-wire routing + aes techmapping). Report `report-E0-MAP5-benchmarks-20260728.md`.
- **E0-MAP5 acceptance note:** the task says "全部基准经完整流程在仿真 fabric 运行正确" (ALL benchmarks run correctly) — this is NOT fully met (2 pass, 3 don't route on v1.1). This is a CHARACTERIZED v1.1 limitation, documented for the maintainer to accept the partial (v1.1 = flow-validation vehicle) or commit to a Phase-1 fabric expansion. **G6: maintainer to decide if Phase-0 exit requires all 5, or accepts pwm+crc32+c432 as the flow validation.**
- **P1 het-fabric Stage-1+2 REVIEW ✅ (findings fixed 2026-07-28):** cross-verified vs C02 §1.3/§2.3 + fabric conventions. **BUG fixed:** `dsp_t.mode_r` was reset-to-0 on `rst_ni` → user reset CLOBBERED the config mode → MAC never accumulated (the "dsp_cfg race" I'd earlier worked around). FIX: `mode_r` PERSISTS across `rst_ni` (fabric convention per elut4/C01 §1: user reset affects virtual state, never config). **GAP fixed:** LAT was build-time (deviated from C02 §2.3 "pipeline is image-selectable") → `eth_inf_dsp_mac` reworked to ALWAYS-3-stage pipeline + **runtime `lat_sel_i` output tap** (0=bypass/1=mult-stage/2,3=out-stage; always-pipelined for good DSP inference); `dsp_t` wires `lat_sel_i = mode_r[2:1]` (runtime). **mem_t mode-degradation documented** (mode_r stored, RAM always basic-mode; full geometry modes = Stage 4, C02 red-lines intact). TB updated (MULT lat_sel=2, MAC lat_sel=3, runtime lat_sel=0 bypass) — ALL PASS, config-persist verified. lint OK / 8 SV TB / 2591 model.
- **P1 het-fabric Stage 4 ✅ (frame_map + fabric_gen heterogeneous, spec-first):** `frame_map.py` — added `mem_tile_type` (mode 16 + vbus_ctrl 22 + vd_i 32 = 70b, C02 §1.3) + `dsp_tile_type` (mode 24 + va 27 + vb 18 + ven 1 + vcasc 48 = 118b, C02 §2.3) + `TILE_LAYOUT` (per-column tile-type map) + layout-aware `pack_column`/`unpack_column`/`blank_column` (base CB108+SB120 + per-type logic; **per-column variable frame length** per C03 §1). `fabric_gen.py` — `tile_types` descriptor → TILE_LAYOUT → `TILE_TYPE` packed param (idx=r*C+c row-major, 8-bit entries) + heterogeneous manifest/frame_map.json. **Reference descriptor `fabric_2x2_het.yaml`** (tile0=MEM/tile1=DSP) → TILE_TYPE=0x201 (verified vs fabric_top convention). **Spec `heterogeneous-config-v0.md`** (frozen per-tile-type config points + per-column geometry + fabric.yaml + layout guidance). 7 new tests; full suite lint OK / 9 SV TB / **2598 passed**. Homogeneous path (bitgen_pack) UNCHANGED (all-CLB default). Report `report-P1-het-framemap-20260728.md`.
- **NEXT (P1):** Stage 5 = **vbus→virtual-routing integration** (tile data through SB/CB to CLBs) + Yosys DSP/RAM inference into synth + VPR heterogeneous arch; Stage 6 = fir16-on-DSP-T chain (C02 ≥10×) + aes-on-MEM-T (S-box ROM) acceptance.
- **P1 het-fabric Stage 5a ✅ (heterogeneous mapping infrastructure):** (a) **Yosys DSP/RAM inference** — `synth_ethereal(heterogeneous=True)` flow `proc; opt; memory -nomap; opt; alumacc; opt; simplemap; abc -lut 4` (VALIDATED: RAM→1 `$mem_v2`, multiply→1 `$macc_v2`, c432→63 $lut+0 hard). Key lessons: `synth`/first-`abc` maps $mem/$mul to gates BEFORE memory/alumacc can collect them; `techmap`/`maccmap` decomposes `$macc_v2` (use `simplemap` instead); `$mem_v2`/`$macc_v2` survive `abc -lut 4`. 4 new synth tests (RAM/MAC/c432-no-hard/homogeneous). (b) **VPR heterogeneous arch** — `arch_ethereal.xml` extended with `mult_27x18` (dsp_t, `.subckt $macc_v2`, A/B/Y) + `mem_2Kx32` (mem_t, `.subckt $mem_v2`, dual-port custom `<model>`, class=memory) tiles in interleaved columns (mult every 4th, mem every 5th, Xilinx/Intel column-per-N style). **c432 routes on the new arch (CPD 5.30ns, Fmax 188.7MHz)** — existing benchmarks unaffected. Yosys emits `.subckt $macc_v2`/`$mem_v2` BLIF models (dual-port $mem_v2 needs custom `<model>`, not built-in single_port_ram). Full suite lint OK / 9 SV TB / **2602 passed**. Report `report-P1-het-map-infra-20260728.md`.
- **NEXT (P1):** Stage 5b = **vbus→virtual-routing integration** (tile data through SB/CB to CLBs — the wide-vbus-mux layer) + heterogeneous bitgen (pack $mem_v2/$macc_v2 onto tiles + route); Stage 6 = fir16-on-DSP-T chain (C02 ≥10×) + aes-on-MEM-T (S-box ROM) acceptance.
- **P1 het-fabric Stage 5b ✅ (heterogeneous bitgen — end-to-end MAC proven):** `bitgen_db.py` extended: `MaccCell`/`MemCell` capture (parse `.net` `mult_27x18[..]`/`mem_2Kx32[..]` + `.place`; bit-blasted port nets order-preserved, a[0]=bit0; operand nets from TOP block ports a/b not the slice's internal a2a). `bitgen_het.py`: `macc_to_dsp_config`/`mem_to_mem_config`/`het_tiles_to_config` (vbus-ctrl operand VALUES = host constants aval/bval/cval for the Stage-5b demo; routed-operand FIR = Stage 6). **Acceptance PROVEN: bitgen DB on MAC circuit (W=48) → macc_to_dsp_config(7×6) → drives REAL dsp_t RTL via iverilog → vp_o == 42.** 5 het-bitgen tests; full suite lint OK / 9 SV TB / **2607 passed**; homogeneous bitgen regression (c432) intact (18 pass). Report `report-E0-MAP3-incr5-hetbitgen-20260728.md`.
- **NEXT (P1):** Stage 6 = **fir16 on DSP-T chain** (16 dsp_t cascade, C02 §2.4 ≥10× throughput) + **aes on MEM-T** (S-box ROM via mem_t rom_init) acceptance — the real heterogeneous benchmarks (the E0-MAP5 ceiling fix realized). Also: `$mem_v2` RAM circuit test (mem_2Kx32 port names best-guess w/ fallbacks — confirm vs arch pb_port).
- **P1 het-fabric Stage 6 ✅ (C02 A1 + A2 metrics MET — the heterogeneous acceptance):** (a) **A1 (AES→MEM-T):** homogeneous LUT-S-box = **4779 eLUT** → heterogeneous MEM-S-box = **290 eLUT + 16 MEM-T** = **16.5× eLUT drop** (C02 §1.6 target ≥5×) — the E0-MAP5 4779-LUT S-box blowup is FIXED (16 S-box ROMs collected into `$mem_v2`). (b) **A2 (FIR→DSP-T):** new `fir16_dsp.v` (16-tap, real multiplier taps, Verilog-2005 flattened-bus ports) → **16 `$macc_v2` (dsp_t cascade) + 0 eLUT datapath** (C02 §2.4: FIR16 = 16 DSP-T cascade; eliminates the v1.1-congested 124-eLUT adder-tree). E0-MAP5 fir16.v was deliberately shift-based (power-of-2) to squeeze v1.1; the real-tap version unlocks the DSP. 2 new C02-metric tests (A1/A2) lock the synthesis-level metrics. Full suite lint OK / 9 SV TB / **2609 passed** / ruff clean. Report `report-P1-het-acceptance-20260728.md`.
- **P1 het-fabric follow-up ✅ (fir16/aes BIT-TRUE — functional loop closed):** `fabric_sim_het.py` (het tile models: `MemTileModel` = ROM comb read + sync-read read-first; `DspTileModel` = signed MAC + acc cascade + lat_sel; `fir16_dsp_eval` = 16-deep dsp cascade; `HetCircuitSim` = LUT glue + mem ROM + dsp MAC fixpoint) + `test_fabric_sim_het.py`. **fir16 BIT-TRUE (32 vec, dsp cascade model vs iverilog fir16_dsp.v golden) + aes BIT-TRUE (32 vec, mem ROM S-box + round model vs aes128_round.v golden).** Honest boundary: proves the mem_t/dsp_t TILE SEMANTICS + fir16/aes compute bit-true; the FULL routed heterogeneous circuit (vbus→routing integration) is separate (Stage 5b used host-constant operands). Debug journey: fir16 golden needed ~18-cycle pipeline wait (16-deep acc chain); AES byte-order fix (LSB-first `state[8*i +: 8]` per the .v, not MSB). 4 het tests; full suite lint OK / 9 SV TB / **2613 passed** / ruff clean. Report `report-P1-het-bittrue-20260728.md`.
- **P1 heterogeneous fabric (user-directed) FUNCTIONALLY COMPLETE:** C02 A1/A2 synthesis metrics (AES 16.5× / FIR 16-DSP) + fir16/aes BIT-TRUE. The base platform now has virtual RAM/DSP blocks alongside eLUT (cross-vendor inference, no vendor primitives), and the fir16/aes compute correctly. **Remaining P1-follow-ups (later phases):** full vbus→virtual-routing integration (routed-operand circuits); Phase-1 rest (BMC/Zynq/IO/runtime per phase-1-GW5 plan).
- **P1 het-fabric Stage 3 ✅ (heterogeneous fabric_top):** `fabric_top` rewritten for heterogeneity: `TILE_TYPE` build-time map (R*C 8-bit entries, 0=CLB_T/1=MEM_T/2=DSP_T) + cfg **unit 2'b11 = TILE-MODE/vbus-ctrl** (MEM_T: intra0=mode, 1=vbus-ctrl(va/ven/vwe), 2=vd_i; DSP_T: intra0=mode, 1=va, 2=vb, 3=ven, 4/5=vcasc hi/lo) + per-tile vbus-control regs + `mem_vd_obs_o`/`dsp_vp_obs_o` observation ports. mem_t/dsp_t instantiated by TILE_TYPE (generate). **Functional TB `tb_het_fabric.sv` PASS** (2x2: MEM_T@tile0 config+RAM write/read CAFEBABE; DSP_T@tile1 config+MULT 7*6=42; CLB@tile2 config+toggle; interconnect intact) — added to `make test-sv` (now **9 TBs**); lint-clean (default all-CLB + heterogeneous). vbus v1: config/obs plumbed; **full vbus→virtual-routing integration + mapping chain = Stage 4-5** (Yosys/VPR target the tiles). Debug journey: vbus-ctrl bit-packing, dsp_obs index (tile1@[95:48] not [47:0]), reset-less mode_r/vcasc X-flush (write mode+operands+vcasc explicitly). Report `report-P1-het-fabric-top-20260728.md`.
- **E0-MAP2 ✅ (VPR arch + native VPR v8.0.0):** `ethereal-tools/tools/mapper/vpr/arch_ethereal.xml` — adapted from VTR's k4_N4_90nm.xml to Ethereal (clb N=8/I=18/O=8, BLE=LUT4+FF+outmux, switch_block `subset`≡classic disjoint, L1 unidir, **fc_in=1.0 faithful** to the implemented full-mux connection_block). VPR v8.0.0 built **natively** (gcc-12; build script `ethereal-tools/tools/mapper/vpr/build_vpr.sh`; runner `run_vpr.sh`). **c432 pack/place/route SUCCESS @ W=12** (real fabric width): 9 CLB / 69 net / 274 wire-seg, **CPD 5.11 ns, Fmax 195.66 MHz**; c17 smoke 0.69 ns/1449 MHz. Acceptance met. Suite no-regression (lint OK / 6 SV TB / 325 mapper test). v8.0.0 build gotchas (force-include limits/algorithm, argparse limits patch, switch_block token=subset not disjoint, explicit `--pack --place --route`) documented. KEY FINDING: c432 needs W=13 at fc=0.25 but routes at W=12 once fc_in=1.0 (faithful to full-mux CB) → validates fabric W=12 suffices for c432. Timing=90nm PTM (not real). Report `report-E0-MAP2-vpr-arch-20260725.md`.
- **E0-FAB3 ✅ (interconnect):** `switch_box.sv` (disjoint unidir W=12, VPR-pending topology) + `fabric_top.sv` (param R×C grid, default 4×4 of clb_t+switch_box+channels); `fabric_model.py` does Kahn cycle-detection on the routing graph → validates **no comb loop** in default config (1×1..4×4..3×5) + detects/breaks a 4-tile ring. Full golden-model suite **1876 pass** locally. v1 CLB↔channel = minimal tap; full CB deferred (S02-P0#1/OCC/VPR). Spec `ethereal-spec/fabric/interconnect-config-v0.md`. Report `report-E0-FAB3-interconnect-20260724.md`.
- **E0-FAB2 ✅ (clb_t):** `ethereal-fabric/rtl/clb/clb_t.sv` (N=8 eLUT4 + flat full-input crossbar IIB, I=26; scoped UNOPTFLAT waiver for the feedback loop); golden model `clb_t_model.py` validated locally (**1218 total pytest** incl. 832-route connectivity exhaustion + toggle-FF + comb-loop detect); cocotb Docker-gated. v1 IIB = **flat crossbar** (frozen + ASSUMPTION — two-level Clos is the v2 optimization). Spec `ethereal-spec/fabric/clb-t-config-v0.md`. Report `docs/reports/report-E0-FAB2-clb-t-20260724.md`.

## Phase summary
| Phase | Window | Core goal | Key exit criteria | Budget |
|---|---|---|---|---|
| **P0** | M0-M2 | Verilator runs full chain (gen fabric→map→config→run→hot-swap), NO vendor tools | dual-image hot-swap pass; AES-128/FIR16 bit-true; ADR-012 archived; CI green | 100-150 p-h |
| **P1** ★1st ext milestone | M2-M5 | GW5 minimal loop: BMC daemon + ethctl + 2-region hot-swap + SPI/I2C dual channel + v0.1.0 | ethctl run/stop/ps/restart all pass; 2×10000 hot-swaps zero-fail; overhead/Fmax published | 150-220 p-h |
| **P2** | M5-M9 | heterogeneous fabric v2 + Zynq port + mFSM + Astral aggregation v1 | **SAME image runs on GW5 & US+ directly**; AES-MEM ≥5×, FIR-DSP ≥10×; aggregation demo; 4 specs v1.0 frozen | 250-350 p-h |
| **P3** | M9-M15 | Service Tile (NPU-Tiny) + scheduling + security v2 + image registry + academic publish | NPU inference demo + container migration; OCI push/pull; paper to FPL/FCCM/TRETS | 300-400 p-h |
| **P4** | M15-M24 | orchestrator + dev experience + Astral full runtime + community gov | unified orchestrator; BMC→Zephyr→Astral node; 4 ref designs; ≥2 core external contributors | milestone |
| **P5** | M24+ | commercial evolution (always complete open core + increments) | enterprise orchestration/RBAC/remote ops; security cert pre-eval; registry SaaS; vendor collab | directional |

## Circuit-breakers (each phase)
- **P0**: VPR arch doesn't converge in 2 weeks → switch to FABulous/nextpnr or self-research placer+router (5 person-day cap each); still fail → fixed W=8 manual topology to protect progress.
- **P1**: BMC FW channel blocked → host direct-drive EMRI/OCC fallback (mFSM semantics first).

## Phase 0 battle sequence (week-by-week)
- **Wk1 foundation**: E0-INF1 (org+8 repos), E0-INF4 (trademark check), E0-INF2 (CI skeleton), E0-INF3 (sim Docker), S04-P0#1 (mailbox RTL migration note)
- **Wk2-3 fabric core**: E0-FAB1 (eLUT4+FF), E0-FAB2 (CLB-T cluster N=8), E0-FAB3 (SB+channel W=12), S02-P0#1 (frame-map gen), E0-FAB4 (OCC v0 WRITE/BLANK/READBACK), E0-FAB5 (blank-before-write + LOCK)
- **Wk3-4 toolchain**: E0-MAP1 (Yosys techlib synth_ethereal), E0-MAP2 (VPR arch XML), E0-MAP3 (bitgen v0), E0-MAP4 (FABulous spike→ADR-012), E0-MAP5 (benchmarks AES/PRESENT/FIR16/CRC32/PWM)
- **Wk5-8 Shell + assembly**: E0-SHL1 (EBI-Tiny), S04-P0#4 (region endpoint + ABI draft), E0-SHL2 (Shell v0 integration), E0-SHL3 (perf model), dual-image hot-swap demo + report

## Key milestones (cross-phase)
- **M-S01-4 (P2)**: SAME image file runs directly on GW5 & Zynq US+ (binary compat first verification)
- **M-S01-2 (P1)**: GW5 runs 3 demo images; overhead/Fmax measured & published
- **M-S03-1 (P0)**: c432 + 5 benchmarks end-to-end bit-true; ADR-012 archived

## Risk register (web-verified)
| # | Risk | P | I | Status/Verification |
|---|---|---|---|---|
| R1 | Fabric overhead/perf fails (>60:1 or <15MHz) | M | H | ZUMA 40:1 is best public baseline (verified); heterogeneous tiles + Landy/Stitt interconnect opt (48-54% reduction) hedge |
| R2 | ~~AE350↔PL interface docs insufficient~~ | — | — | **ELIMINATED** (v2.1 deprecates AE350 → NEORV32 soft-core) |
| R3 | VPR arch file + bitgen workload explodes | M | H | VPR/VTR MIT, XML arch mature (verified); OpenFPGA two-level bitstream ref; circuit-breaker sensible |
| R4 | Gowin EDA commercial license cost/limits | L | M | Apicula+nextpnr-himbaechel Aurora V supports GW5 (verified, experimental) |
| R5 | MIT license patent exposure | L | M | DCO Signed-off-by mitigates; ADR-005 decided |
| R6 | Solo maintainer burnout | M | H | Each phase produces independently usable artifact + strict exit criteria hedge |
| R7 | ZUMA/FABulous license conflict | L | M | ZUMA paper method not copyrighted (reimplemented); FABulous Apache-2.0 borrow arch ideas no conflict (not direct code merge) |
| R8 | "Container" semantics questioned (no real multi-tenant security) | M | L | Docs clear: v1 prevents accidents, v3+ prevents attacks; overlay structural scan capability (L3) is security bonus reserve |
| R9 (new v2.1) | NEORV32 timing/primitive-inference quirks in Gowin synth | M | L | Community Gowin ports exist (neorv32-setups osflow); fallback core VexRiscv (E2-BMC2) |

## ⚠️ ZUMA routing-width citation discrepancy
Original v2.0 §3.3 cites "ZUMA W=12" but ZUMA paper actually says "routing width fixed at **112**" (total routing tracks). Phase 0 VPR experiment (E0-MAP2) should clarify whether "per-direction W" or "total tracks" to avoid scaling error. Citation precision issue, doesn't affect methodology.

## AE350 deprecation side-effect
v2.1 deprecates → GW5's DDR3 1GB + PCIe 3.0 hard-core NOT directly used in Phase 1 (BMC uses BSRAM/SSRAM); DDR3 access pushed to Phase 3 (image pool expansion); risk table dependency needs update.

## Open questions for user (from subsystem §7, by priority)
| Priority | Question | Blocks |
|---|---|---|
| 🔴 high | **"Astral" naming conflict** — `github.com/AstralPlatform` (no hyphen) is an active FPGA/RISC-V org; `astral-os` already taken; astral.sh (Ruff/uv) dominates software mindshare. Use `Astral-OS`+tagline or rename? | `astral-os/` repo, branding (E0-INF4 report) |
| 🟡 mid | Mailbox RTL G1-cleanup backlog (~22 procedural loops, plain-logic FSMs, no nettype restore) — add task `S04-P0#2`? Cleaned RTL only then enters main `make lint` gate. | `S04-P0#2` |
| ✅ resolved | ~~Mailbox re-license + migrate out of TinyGPU-FPGA~~ → **DONE 2026-07-24** (S04-P0#1) | — |
| 🔴 high | Exact Zynq US+ board model (constraints + DFX slot planning)? | E2-PLT1 |
| 🔴 high | Tang Mega 138K = Dock (confirmed) or Pro? (Board Manifest pin table) | E1-IO3, E1-PLT2 |
| 🟡 mid | Final virtual LUT granularity = LUT4? (affects all downstream; sim can parametrize LUT6 compare) | E0-FAB1 |
| 🟡 mid | ADR-012 MAP route A(VPR) vs B(FABulous); accept dual-track? | E0-MAP4 |
| 🟡 mid | Profile-E first small-device target (GW5AT-15? GW2A-18?) | E2-BMC1 |
| 🟡 mid | BMC FW v1 bare-metal vs Zephyr directly? (recommend bare-metal start) | E1-BMC2 |
| 🟡 mid | ethctl compose YAML syntax = docker-compose subset? | E2-AST1 |
| 🟢 low | L2 proxy v1 third protocol (SPI master or I2C master)? | E2-IO1 |
| 🟢 low | Astral full runtime: separate project (S15-S18) now or after Phase 2? | E2-AST1 |
| 🟢 low | Paper vs product priority? (overlay paper costs 2-3 months but academic backing) | E3-PUB1 |
